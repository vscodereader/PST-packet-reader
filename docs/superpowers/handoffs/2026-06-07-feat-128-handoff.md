# 핸드오프 — feat/128 로그 일원화 (이슈 #128, Issue B)

> 작성: 2026-06-07 (노트북). 내일 회사 PC에서 이 작업을 이어가기 위한 인수인계.
> 이 파일은 PR 만들기 전에 `git rm` 해도 되고, 둬도 CI에 영향 없음(.md라 test-required 미해당).
> squash 머지를 쓰므로 남겨도 최종 master 이력은 깔끔함.

---

## 0. 한 줄 요약

이슈 **#128 "로그를 pstmacro.log 하나로 통합 + 가독성·상세화"(Issue B, 백엔드)**의
구현(Task 1~5)을 **전부 끝내 `feat/128`에 커밋·push 완료**. 남은 건 **PR 생성(Task 6)**뿐.
PR은 의도적으로 안 만들었음(사수 알림을 내일 회사에서 내려고).

---

## 1. 큰 그림 (이 작업이 어디서 왔나)

- 출발: "에러 메시지를 일반인 친화적으로 + 로그를 한 파일로" 설계 → **이슈 2개로 분할**
  - **Issue A (#127, `feat/127`)** — 프론트 토스트 친화화. `friendlyError()` 매핑.
    `feat/127`은 origin에 push돼 있음. **아직 PR 없음.** (이 핸드오프 범위 밖, 별건)
  - **Issue B (#128, `feat/128`)** — 백엔드 로그 일원화. **← 지금 이 작업.**
- 설계서/계획서 (origin `docs/error-logging-spec` 브랜치에 push해 둠):
  - `docs/superpowers/specs/2026-06-07-error-messages-and-logging-design.md` (설계, 메시지 인벤토리 126개)
  - `docs/superpowers/plans/2026-06-07-log-consolidation.md` (**Issue B 구현계획 Task 1~6 — 이게 핵심**)
  - `docs/superpowers/plans/2026-06-07-friendly-toast-messages.md` (Issue A 계획)
  - ⚠️ 이 문서들은 `feat/128` 트리엔 없음. 보려면:
    `git show origin/docs/error-logging-spec:docs/superpowers/plans/2026-06-07-log-consolidation.md`

## 2. ⚠️ 의존성 — 반드시 알 것

- `feat/128`은 **`fix/code-review-followup`(PR #126, 사수 작업) 위에 쌓여 있음.** (베이스 커밋 `1d19de9` 포함)
- **#126은 아직 master에 머지 안 됨(OPEN).**
- 계획 원칙: **#126이 master에 머지된 뒤 → `feat/128`을 master로 rebase → 그다음 #128 머지.**
- 즉 내일 PR은 올릴 수 있지만, **머지는 #126 머지 이후.** 사수가 #126을 고치면 그에 맞춰 rebase 필요.

## 3. 무엇을 했나 — 커밋 4개 (전부 origin/feat/128에 push됨)

| 커밋                        | 계획 Task | 내용                                                                                                                                                    |
| --------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `6173639`                   | Task 1    | 로그 타임스탬프를 **로컬 시각 `YYYY-MM-DD HH:MM:SS`** 로 (chrono `LocalTimer`, logging.rs). UTC 마이크로초 폐기                                         |
| (Task 2는 아래 커밋에 포함) | Task 2    | `src-tauri/src/auth/util.rs` 에 `mask_id()` 헬퍼 + 테스트 (`cho****`, 3자 미만 `****`)                                                                  |
| `b084048`                   | Task 3    | **`queue.log` 파일 출력 제거** → 로그인 큐 이벤트를 `tracing`으로 `pstmacro.log`에 합류. `#순번 cho****` 마스킹. in-memory `inner.logs`(UI 계약)는 유지 |
| `84c44d6`                   | Task 4    | **PW 비노출 보안 테스트** (`never_emits_password...`) — 비밀번호가 로그에 안 들어감을 단언                                                              |
| `c32060d`                   | Task 5    | **글/댓글 작업 결과 로그** (`[POST]` 줄) — 아래 4절 상세                                                                                                |

원칙 전체: **기능·제어 흐름 무변경, 로그 출력만 추가/이동. PW는 어떤 로그에도 안 들어감.**

## 4. Task 5 상세 (오늘 마지막으로 한 것, 커밋 `c32060d`)

`src-tauri/src/discussion_batch.rs` — 종목토론방 게시 **완료 지점이 2개**라 둘 다 로깅:

1. `run_forum_publish` (즉시 게시, `run_forum_publish_now` 커맨드 경로) — 종목 루프 끝에 결과 로그.
2. `run_discussion_batch` (N회 배치, `run_naver_discussion_batch` 커맨드 경로) — 3분기(글+댓글/글/댓글) 각각 로그.
   - `?` 조기반환을 **동등한 `match`로 교체**해서 실패도 로깅 (Err 시 같은 `Err(message)` 반환 — 동작 동일).

`src-tauri/src/auth/mod.rs` — `pub(crate) use util::mask_id;` 재노출 (discussion_batch에서 사용).

로그 형식 예:

```
[POST] cho****  "삼성전자" 종목토론방 글+댓글 성공 ✅
[POST] cho****  "삼성전자" 종목토론방 댓글 실패 ❌ — <사유>
```

계정 ID는 `mask_id`로 마스킹, PW 비노출, `#순번`은 이 경로에선 생략(계획이 허용).

## 5. 검증 결과 (오늘 노트북에서 실행)

- `cargo build` ✅ exit 0, 경고 없음
- `cargo test` ✅ 530 + 통합 13 + doc 3 전부 통과, 0 실패
- `rustfmt --check` (Windows `rustfmt.exe`) ✅ 변경 두 파일 exit 0
- `cargo clippy` — ⚠️ **이 WSL 환경엔 미설치**(Windows 툴체인/CI에만 있음). **내일 회사에서 `cargo clippy -- -D warnings` 한 번 돌릴 것.**
  - 환경 메모: 코드=WSL(리눅스 cargo, build/test OK) / fmt·clippy=Windows `/mnt/c/.../.cargo/bin/*.exe` / 테스트=Windows.

## 6. 양식 준수 현황

- 이슈 #128: 레포 `feature_request.md` 템플릿(Problem/Proposed solution/Alternatives/Additional context) 준수, 라벨 `enhancement`, 제목 `feat:` ✅
- 브랜치 `feat/128`: CI 규칙 `^(feat|...)/(\d+|...)$` 통과 ✅
- 커밋 4개: conventional commit + commitlint 통과 ✅
- PR: **미생성(내일 할 일).**

## 7. 남은 일 (내일)

1. **(권장) `cargo clippy -- -D warnings`** 한 번 — WSL에서 못 돌린 유일한 게이트.
2. **PR 생성** (아래 8절 명령) — 제목 conventional + 본문 `Closes #128`.
3. **사수 리뷰요청** — Reviewers에 사수 지정 또는 새 코멘트로 `@사수`. (PR 본문 수정으로 @멘션 추가하면 알림 안 감 주의.)
4. **머지는 #126 머지 이후.** #126 머지되면 `feat/128`을 master로 rebase 후 머지.
5. **별도 정리(이 PR과 분리):** #119(`feat/116`) PR에서 "queue.log 가독성" 항목/코드 제거
   (queue.log는 #128에서 없앴으므로). 별도 커밋:
   `chore(forum): drop queue.log readability from feat/116 (moved to issue B)`.

## 8. 내일 PR 올리는 명령 (그대로 복붙)

```bash
# 0) 최신화 + 브랜치 진입
git fetch origin
git checkout feat/128
git pull --ff-only            # origin/feat/128 동기화

# 1) (선택) 이 핸드오프를 PR에서 빼고 싶으면
git rm docs/superpowers/handoffs/2026-06-07-feat-128-handoff.md
git commit -m "chore: drop handoff note"

# 2) clippy 한 번 (WSL에서 못 돌린 게이트)
cd src-tauri && cargo clippy -- -D warnings && cd ..

# 3) PR 생성 (Closes #128 필수)
gh pr create --base master --head feat/128 \
  --title "feat(logging): 로그 pstmacro.log 일원화 + 가독성·상세화" \
  --body "$(cat <<'EOF'
## Summary

- queue.log 파일 출력 제거 → 로그인 큐 이벤트를 pstmacro.log로 합류(로그 파일 1개)
- 타임스탬프 로컬 시각화, 마스킹 식별자(#순번 cho****), 로그인·글/댓글 결과 로그 추가
- PW는 어떤 로그에도 비노출(never_emits_password 테스트). 기능·로직 무변경

Closes #128

## ⚠️ 의존성
- 이 브랜치는 #126(fix/code-review-followup) 위에 쌓임. **머지는 #126 머지 이후.**

## Test plan
- [ ] cd src-tauri && cargo clippy -- -D warnings && cargo test
- [ ] pnpm test (프론트 변경 없음)
- [ ] CI green
EOF
)"

# 4) 사수 리뷰요청 (둘 중 하나)
gh pr edit feat/128 --add-reviewer <사수-github-id>
# 또는: gh pr comment feat/128 --body "@<사수-github-id> 리뷰 부탁드립니다"
```

> 머지(나중에, #126 머지 후): `gh pr merge --squash --delete-branch`

## 9. 내일 Claude Code를 부팅할 때 줄 첫 프롬프트(예시)

> "이 레포 `feat/128` 브랜치에서 이슈 #128(로그 일원화) 작업을 이어간다.
> `docs/superpowers/handoffs/2026-06-07-feat-128-handoff.md`를 먼저 읽고,
> `git log origin/feat/128`과 `gh issue view 128`로 현재 상태를 확인한 뒤,
> 남은 일(7절: clippy 확인 → PR 생성 → 사수 리뷰요청)을 진행해줘.
> 계획서는 `git show origin/docs/error-logging-spec:docs/superpowers/plans/2026-06-07-log-consolidation.md`."
