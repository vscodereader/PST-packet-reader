# Admin–하위 원격제어(SSE) 설계 — 대화 전체 기록

- 기록일: 2026-06-27
- 대상 문서: `docs/설계.md` (Admin–하위 컴퓨터 원격제어 계획서, SSE 기반)
- 목적: 설계서를 구현으로 옮기기 전, 무엇을 임의로 만들 수 있고 무엇을 사람이 결정해야 하는지 가려내고, 네트워크/서버 구조 개념을 정리한 전 과정을 누락 없이 보존.
- 성격: 사용자(개발자, 인프라 비전문)와 Claude의 Q&A 전체 로그를 주제별로 충실히 재구성. 임의 판단 배제 원칙으로 진행됨.

---

## 0. 진행 원칙 (사용자 지시)

- "정확히 구현하기 위해 **Claude는 임의 판단하지 말고 전부 사용자에게 물어볼 것.**" — 그래야 사용자가 사수에게 정확히 지시를 받을 수 있음.
- 회사에 **인프라팀이 없음 → 사용자가 전부 직접 개발.** 따라서 "인프라팀에 문의" 류 항목은 전부 사용자(개발) 결정 또는 사수의 업무/정책 결정으로 재분류.
- PR은 요청 시에만 생성. 평소엔 브랜치 푸시까지만(메모리 `pr-on-demand-only`).

---

## 1. 첫 요청 — "어디까지 대화했는지 전부 기억해서 말해달라"

### Claude의 솔직한 전제

이전 대화 본문이 현재 컨텍스트에 남아있지 않아, **추측 대신 남은 흔적(git 기록 + 작업 중인 `docs/설계.md` + 메모리)으로 복원**함을 먼저 밝힘.

### 복원된 작업 상태

작업 대상: **`docs/설계.md` — "Admin–하위 컴퓨터 원격제어 계획서 (SSE 기반)"** 한 문서.

- 브랜치: `docs/admin-remote-control-design`
- 사수 지시: **계획서만 작성, 구현 보류.** 기존 pstmacro는 무손상으로 두고 그 위에 ①하위(기존 Tauri 앱 + 에이전트 레이어) ②Admin(신규 웹)만 _추가_.

#### 커밋 진행 순서 (오래된 → 최신)

1. `e07056f` — 설계.md 최초 추가 (원격제어 계획서 골격)
2. `7c88358` — §10-3 계정 분배 화면 + 랜덤 균등 분배 추가
3. `1a1b059` — 계정 스테이징·MOVE 모델·결과보고(§10-4)·SuperAdmin 부트스트랩 반영
4. `0132a21` — 역할 분리 명시 (기존 무손상 + 하위=기존앱+에이전트, Admin=신규 웹)

#### 당시 커밋 안 된 변경 (작업 트리)

"자동 로그인 플랫폼 = 종토(종토방)로 확정"을 3군데 추가(아직 커밋 전이었음):

- §10-2 분배 규칙에 "분배 대상 계정 = 종토 가정, 종토 선택 로그인 경로(`isSelectiveLoginPlatform`/`buildLoginNowItem`, #228) 그대로 재사용" 추가
- §10-3 흐름도에 "(종토 선택 로그인 경로 재사용)" 명시
- §13 "이번에 확정됨"에 종토 확정 항목 추가

#### 설계의 핵심 합의 (당시 문서 상태)

- **통신**: 하위가 SSE(`GET /agent/stream`)로 명령 수신, 결과·하트비트·IP는 HTTP POST. 연결을 여는 쪽은 **항상 하위**(CGNAT·IP 변경 대응). 신원은 IP가 아니라 **기기토큰(device_id)** 으로.
- **계정 소유(A-1 확정)**: Admin 스테이징 → 분배 시 하위로 **MOVE**(보낸 계정은 Admin에서 사라짐). 쿠키·로그인은 중앙 금지, 각 하위가 자기 폰 IP로.
- **분배**: 체크박스 수동 선택 → **균등+랜덤**. 기존 `distributeStocksEvenly()` 재사용 + 분배 직전 셔플 1줄만 신규.
- **결과 보고(§10-4)**: 성공/보류/대기초과/실패 4분류, 실패는 자동삭제(보고 후), 배치별 상세 + 누적 합계.
- **권한(§5)**: 운영자=SuperAdmin 동일 권한, 단 "운영자 삭제"만 SuperAdmin 전용. 부트스트랩 `Superadmin`/`Superadmin` → 강제 비번 변경. 비번 분실 복구·이메일 없음.

#### 부차 발견

`docs/설계.md:Zone.Identifier` 라는 빈 파일이 untracked로 존재 — Windows 다운로드 찌꺼기.

---

## 2. 두 번째 요청 — "이 설계서로 구현 시, 임의 판단 가능한 곳 vs 봐도 설계 모르겠는 곳은? 기존 로컬코드 싹 다 읽고 답하라"

### 접근

설계서가 인용한 구체 파일·함수·라인이 실제로 존재하고 인터페이스가 명확한지부터 검증하기 위해, **5개 병렬 탐색 에이전트** 투입:

- A. Auth + IP 로테이션 + 로그인 결과 (Rust)
- B. 계정 데이터 + 엑셀 임포트 + 삭제 + 쿠키 + 저장 (Rust)
- C. 선택 로그인/종토 + queue_runner + 명령 와이어링 + lib.rs
- D. 프론트엔드 — IPC transport seam + accounts.tsx + 분배 헬퍼 + features 구조
- E. 기술 스택/빌드/서버 인프라/의존성

### 에이전트 A 결과 — Auth / IP 로테이션 / 로그인 결과

| 항목                               | 위치                                              | 상태                    | 비고                                                                                                                                                                                                                                                                                                           |
| ---------------------------------- | ------------------------------------------------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------- | -------------------------------- |
| `toggle_airplane_mode()` 반환·단계 | `auth/adb.rs:82–125`                              | ✅ VERIFIED             | `async fn() -> Result<IpRotation, OrchestratorError>`. 반환 `IpRotation { before, after, changed }` — 설계엔 없던 `changed: bool` 추가 존재. 단계: fetch_external_ip(ipify) → airplane ON → wait_until_phone_offline(ping 폴) → airplane OFF → wait_for_internet_connection → wait_for_ip_change. 설계와 일치. |
| login_flow.rs ROTATING/READY 훅    | `auth/login_flow.rs`                              | ❌ GAP                  | 모든 함수/타입이 `pub(crate)` 내부 전용. ROTATING/READY 상태를 외부에 통보하는 훅이 **실제로 없음.** 에이전트 연동하려면 `process_account()`에 콜백/이벤트 신설 또는 `auth/mod.rs`에 래퍼 노출 필요.                                                                                                           |
| `manual_captcha` 플래그(~120s)     | `auth/login.rs:25`, `login_flow.rs:21–25,176–186` | ✅ VERIFIED             | `MANUAL_CAPTCHA_TIMEOUT = 120s`. 첫 로그인=캡차 뜨면 즉시 OnHold로 떨굼(FailCaptchaToHold). 보류 재로그인(manual_captcha=true)=창 열어 120초 사람 대기(WaitCaptcha). `auth/mod.rs:93–101`에서 현재 상태가 OnHold면 manual_captcha 결정.                                                                        |
| outcome.rs 상태 enum               | `ipc/accounts.rs:40–61`                           | ✅ VERIFIED (+추가상태) | `AccountStatus { New, Active, Waiting, OnHold, TimedOut, BadCredentials, Challenge, Blocked, Error }` — 9종. 설계는 7종 가정. `New`(초기), `Waiting`(게시 후 대기)는 설계 미기재. 매핑(outcome.rs:99–115): Ok→Active, ChallengeRequired→Challenge, BadCredentials→BadCredentials, Blocked                      | Protected | Locked→Blocked, CaptchaUnsolved | PhoneVerify→OnHold, Error→Error. |
| 로그인 트리거·결과 표면            | `queue_runner.rs:2253–2266`, `outcome.rs:13–22`   | ✅ VERIFIED             | `queue_runner`가 `auth::process_account(...)` 호출 → `LoginResolution { status, message, succeeded, trace }` 반환 → accounts JsonStore에 status/status_msg 기록 → 프론트가 폴링으로 봄.                                                                                                                        |

**핵심 GAP**: 설계 §9가 말한 "로테이션 직전/직후 에이전트 상태 통보 훅"이 **코드에 없음.** login 관련 함수 전부 `pub(crate)`라 에이전트 레이어는 크레이트 내부에 있거나 래퍼를 노출해야 함.

### 에이전트 B 결과 — 계정/엑셀/삭제/쿠키/저장

| 항목                    | 위치                            | 상태                           | 비고                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------- | ------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `import_accounts(path)` | `excel.rs:76–159`               | ✅ VERIFIED / ⚠️ 반환 MISMATCH | 시그니처: `pub fn import_accounts(path: &str, mut existing: Vec<Account>) -> Result<(Vec<Account>, ImportSummary), String>`. calamine로 **로컬 파일경로만** 읽음(JSON/인메모리 변형 없음). 반환은 `ImportSummary { imported, skipped, errors }` — 설계가 가정한 "loginIds 목록"은 명시 반환 안 됨(반환된 `Vec<Account>`로 접근 가능). 등록완료 게이트는 `imported+skipped==K` 또는 store 재조회로 처리. |
| `delete_accounts(ids)`  | `accounts.rs:228–237`           | ✅ VERIFIED                    | `#[tauri::command] pub fn delete_accounts(store, activity, ids: Vec<String>) -> Vec<Account>`. `apply_delete()`로 필터 제거 후 갱신 목록 반환.                                                                                                                                                                                                                                                          |
| 계정 STORE              | `store.rs`, `accounts.rs:67–80` | ✅ VERIFIED                    | 위치 `{APP_ROOT}/accounts/accounts.json`. `JsonStore<T>` = 인메모리 `Vec<T>` + Mutex, mutate마다 JSON 파일 전체 저장. **DB 없음, 순수 JSON.** Account 스키마: `id, platform(PlatformId enum), login_id, pw, status, status_msg?, last, tags`. loginId 조회는 `apply_status_by_login_id()`(같은 loginId 다중 행 가능).                                                                                   |
| 쿠키                    | `auth/accounts.rs:112–116`      | ✅ VERIFIED                    | `{APP_ROOT}/cookies/{account_id}.json`. 형태: `{ "cookies": [ {name, domain, expires, value} ] }`. 필수: NID_AUT, NID_SES. **로컬 전용.**                                                                                                                                                                                                                                                               |
| 비번 at-rest            | `accounts.rs:71`                | ✅ VERIFIED (평문)             | `pw: String` — **평문 저장.** 코드 전체에 encrypt/decrypt/aes/chacha 없음.                                                                                                                                                                                                                                                                                                                              |
| 상태 영속성             | `accounts.rs:72,115–131`        | ✅ VERIFIED                    | status는 transient 아님, 레코드에 영속. `apply_status_by_login_id()`로 갱신.                                                                                                                                                                                                                                                                                                                            |

**추가 발견**: Tauri 커맨드명은 `import_accounts_xlsx`(lib.rs)이고 저수준 `excel::import_accounts()`와 다름. "loginId로 단건 조회" 전용 함수는 없음.

### 에이전트 C 결과 — 선택 로그인/종토/queue_runner/lib.rs/플랫폼 모듈

| 항목                                             | 위치                                                                                 | 상태                     | 비고                                                                                                                                                                                                                                                                         |
| ------------------------------------------------ | ------------------------------------------------------------------------------------ | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ---- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `isSelectiveLoginPlatform` / `buildLoginNowItem` | `src/features/accounts/login-queue.ts:11,30`                                         | ✅ VERIFIED              | `isSelectiveLoginPlatform(platform): boolean` → `forum                                                                                                                                                                                                                       |     | blog |     | clip`참.`buildLoginNowItem(targets: Account[], id): QueueNowItem`. 순수함수, `useAdb:true, force:true`. accounts.tsx:376에서 사용. 테스트 `login-queue.test.ts:93–100`. 설계는 "종토 재사용"인데 코드는 blog+clip까지 확장(하위호환). |
| queue_runner 진행률 방출                         | `queue_runner.rs:3624–3641`                                                          | ✅ VERIFIED / ⚠️ PARTIAL | `update_progress(app,id,done,total)` → JsonStore에 `progress: Option<(u32,u32)>` 저장. **Tauri emit 없음, 프론트 폴링**(`list_queue_now`). 진행률이 **item 단위지 step 단위 아님** → 설계 §10-1의 "단계별 같은 commandId 진행률"엔 step 메타 추가 필요.                      |
| lib.rs invoke_handler                            | `lib.rs:646–706`                                                                     | ✅ VERIFIED              | Tauri 커맨드 **61개** 등록. 도메인별(accounts 5, posts 3, queue 9, cafes 7, diagnostics 3, band 3, naver_discussion 3, forum_stocks 2, stocks 2, activity 2, export/import 4, autostart 2, 기타 8). 모두 stateless, 제너릭 래핑 가능.                                        |
| 플랫폼 모듈 무손상                               | `src-tauri/src/{naver_cafe,naver_blog,band_auth,band_post,naver_clip,forum_stocks}/` | ✅ VERIFIED              | 전부 존재. naver_cafe=orchestrator.rs(로그인+게시 루프), naver_blog=댓글전용(카페 쿠키 재사용), band_post=client.rs+signature.rs(MD 서명), naver_clip=댓글전용, forum_stocks=CDP 패킷엔진. 설계 §9 "수정 대상"(login_flow/adb, queue_runner, lib.rs)에 안 들어가므로 무손상. |
| 프론트 IPC 패턴                                  | `src/shared/ipc/index.ts:135–136`                                                    | ✅ VERIFIED              | `function call<T>(cmd, args?) { return invoke<T>(cmd, args) }`. 모든 `ipc.*`가 이 한 점을 통과. transport seam 슬롯인 지점 명확.                                                                                                                                             |

### 에이전트 D 결과 — 프론트엔드 transport/accounts/분배

| 항목                        | 위치                       | 상태                         | 비고                                                                                                                                                                                                                                                             |
| --------------------------- | -------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/shared/ipc` chokepoint | `index.ts:1–362`           | ✅ VERIFIED / ⚠️ 추상화 없음 | `ipc` 객체가 모든 커맨드 래핑. **features에 직접 `invoke()` 0건**(테스트 목 제외) — 매우 깨끗한 seam. 단 `Transport` 인터페이스(Tauri/HttpSse)는 **아직 없음** → 신설 필요. accounts.tsx:21이 `@tauri-apps/plugin-dialog`의 open/save 직접 import(웹 대체 필요). |
| `distributeStocksEvenly()`  | `publish-helpers.ts:28–44` | ✅ VERIFIED                  | `(codes: string[], buckets: number) => string[][]`. 각 버킷 ⌊N/M⌋, 나머지는 **앞 버킷**에 +1, **100% 결정적**(`b < remainder ? 1 : 0`). 테스트 `publish-helpers.test.ts:15–22`: 18/4 → [5,5,4,4] 존재. 설계의 "분배 직전 셔플" 접근 옳음(함수 무수정).           |
| accounts.tsx UI 라인        | `accounts.tsx`             | ✅ VERIFIED (드리프트 0)     | 계정추가 `:634`, 개수 "총 N개 계정" `:864`, 헤더 전체선택 `:737–745`, 행 체크박스 `sel` 배열 `:770–780`. 패턴 재사용 가능.                                                                                                                                       |
| features 디렉터리           | `src/features/`            | ✅ VERIFIED                  | `accounts, dashboard, notifications, posts, queue` 5개 — 설계와 정확히 일치.                                                                                                                                                                                     |
| accounts.tsx 결합도         | `accounts.tsx`             | ⚠️ MEDIUM                    | IPC는 전부 `ipc.*` 경유(낮은 결합). 단 파일 dialog(open `:525`, save `:560`)는 Tauri 전용 → 웹은 `<input type=file>`+fetch로 대체 필요.                                                                                                                          |

**최고 가치 발견**: transport seam이 충분히 깨끗함. `src/shared/ipc/transport.ts`에 `Transport` 인터페이스(`invoke`/`subscribe`) 도입 + `TauriTransport`/`HttpSseTransport` 2구현 + 파일 dialog 추상화면 됨. 컴포넌트 로직 변경 없음. 추정 2~4일.

### 에이전트 E 결과 — 스택/빌드/서버 인프라

- **구조**: 루트 `Cargo.toml` 없음 = **단일 크레이트**(워크스페이스 아님). `server/` 형제 크레이트 추가하려면 **워크스페이스 전환 필수**.
- **백엔드 기존 의존성(재사용 가능)**: `tokio(full)`, `reqwest 0.12(blocking,json,rustls-tls,gzip)`, `serde/serde_json`, `thiserror`, `tracing(+subscriber,+appender)`, `chrono`, `hmac/sha2/base64`(Band HMAC), `tungstenite 0.21`(forum WS).
- **백엔드 없어서 추가해야 함(net-new)**: 웹 프레임워크 `axum`, SSE(`tokio-stream`/axum 내장), DB(`sqlx` 또는 `rusqlite`), JWT(`jsonwebtoken`), 비번 해시(`bcrypt`/`argon2`), `uuid`/`rand`, at-rest 암호화(`aes-gcm`/`chacha20poly1305`).
- **프론트**: React 19.2.7 + Vite 7.0.4 + Mantine 9. `@tauri-apps/api ^2`, `@tauri-apps/plugin-dialog ^2.7.1`. **EventSource/SSE·HTTP 클라이언트·상태관리 라이브러리 없음** → 브라우저 네이티브 `EventSource`/`fetch`로 충분, 상태관리는 선택(zustand).
- **tauri.conf.json**: 데스크톱 전용. CSP `connect-src 'self' ipc:` → 웹 모드면 서버 origin 추가 필요.
- **기존 네트워킹 재사용 힌트**: `naver_automation/packet_client.rs`, `band_post/client.rs`(reqwest 패턴). IP 로테이션 훅 지점 `auth/adb.rs`. 크립토 `hmac/sha2/base64`(JWT 시크릿 전용 가능). 영속성 패턴 `store.rs`.
- **빌드/실행**: `pnpm dev`(Vite :1420), `pnpm tauri dev/build`, `pnpm test:rust`(cargo, 커버리지 83% lines).

### 종합 결론 — 3갈래 분류

#### A. 임의 판단으로 바로 만들 수 있는 곳 (코드 존재 + 인터페이스 명확)

| 부분                   | 근거                                                                                                        |
| ---------------------- | ----------------------------------------------------------------------------------------------------------- |
| 균등+랜덤 분배         | `distributeStocksEvenly` `publish-helpers.ts:28–44`, 결정적, 테스트 18/4→[5,5,4,4]. 함수 무수정 + 셔플 1줄. |
| 분배 화면 UI           | `accounts.tsx` :634/:864/:737–745/:770–780, 드리프트 0. 8행 스크롤 스타일만 추가.                           |
| 하위 자동 로그인 경로  | `isSelectiveLoginPlatform`/`buildLoginNowItem` `login-queue.ts:11,30`, 순수함수, 종토 지원.                 |
| 계정 등록              | `import_accounts(path, existing)` `excel.rs:76`. (반환은 카운트만 — 게이트는 카운트 비교/재조회)            |
| 실패 자동삭제          | `delete_accounts(ids)` `accounts.rs:228`, 상태 enum 9종.                                                    |
| IP 로테이션 트리거     | `toggle_airplane_mode() -> IpRotation{before,after,changed}` `adb.rs:82–125`.                               |
| Transport seam(프론트) | `index.ts:135` 단일 통로, features 직접 invoke 0건.                                                         |
| 명령 표면              | `lib.rs:646–706` 61 커맨드, stateless.                                                                      |

#### B. 결정만 받으면 만들 수 있는 곳 (선택지 좁음, Claude 추천+진행 가능)

1. 워크스페이스 전환(루트 Cargo.toml 신설).
2. 서버 스택 선택(추천: axum + rusqlite + jsonwebtoken + argon2 + aes-gcm).
3. 로그인 상태 훅 노출(`process_account()`에 콜백/이벤트).
4. 진행률 per-step화(`progress`에 step 태그).

#### C. 봐도 어떻게 설계할지 모르겠는 곳 (사람 결정/환경정보/스파이크 필요)

1. 🔴 Admin 웹 배포 토폴로지(서버 위치/주소/TLS/포트/방화벽) — 코드 단서 0.
2. 🔴 자격증명 at-rest 암호화 방식(현재 로컬 평문; 키 보관·회전·접근통제).
3. 🟡 데스크톱 전용 호출의 웹 대체(파일 dialog 등; 스파이크 필요).
4. 🟡 분배 계정 전달 형식(import_accounts가 파일경로만; 임시 xlsx vs store 직삽 신규경로).
5. 🟡 동시 조작 충돌 정책(현장 사람 + Admin; 코드에 개념 없음).
6. 🟡 ROTATING 중 명령 / 비번분실 / 재등록 토큰 무효화 — 신규 정책.

**한 줄 요약**: 설계서가 크게 _틀린_ 곳은 없음. 가장 큰 *공백*은 ① 서버 인프라가 통째 net-new(axum/DB/JWT/암호화 전무) ② `login_flow`에 상태 훅이 실제로 없음.

---

## 3. 세 번째 요청 — "지금까지 내용 커밋하고 내 브랜치에 올려줘"

- 작업: 떠 있던 "종토 확정" 변경(설계.md 3곳)만 스테이징. `Zone.Identifier`는 제외.
- 커밋: `2af4a8a` — `docs: 자동 로그인 플랫폼=종토 확정 — 종토 선택 로그인 경로 재사용(§10-2·10-3·13)` (lint-staged prettier 통과).
- 푸시: `0132a21..2af4a8a` → `origin/docs/admin-remote-control-design`. pre-push 통과(테스트 없음). **PR 생성 안 함**(요청 시에만).
- `docs/설계.md:Zone.Identifier`는 여전히 untracked로 남음(정리 제안만).

---

## 4. 네 번째 요청 — "임의 판단 말고, 내가 사수한테 정확히 물어볼 게 뭔지 다 알려줘"

실측 기반으로 사수에게 그대로 읽어 줄 질문지를 작성. 🔴 착수 불가/🟡 곧 필요/⚪ 나중 가능으로 표시. (이후 5번에서 인프라 항목은 대거 정정됨에 유의.)

### 1. 배포·인프라 🔴 (당초)

1. 서버를 어디에 띄우나요? Admin 컴퓨터와 같은 머신인가요, 별도 서버인가요?
2. 하위가 접속할 주소는 고정 공인 IP 직접인가요, 도메인인가요?
3. HTTPS(TLS)를 씁니까? 인증서는 자체서명/Let's Encrypt/사내 CA 중?
4. 폰 테더링 망에서 서버 포트로 outbound 접속이 실제로 열리나요?
5. Admin 웹도 이 서버가 같이 서빙하나요, 별도 호스팅인가요?

### 2. 보안·자격증명 🔴

6. 계정 ID/PW를 서버에 모으는 것 자체를 승인하나요?(현재 로컬 평문)
7. 암호화 키를 어디에 두나요? 키 회전은?
8. 분배 화면에서 운영자가 비번 평문을 봐야 하나요, 마스킹인가요?
9. 서버 유출 시 전 계정 비번 노출 — 감수하고 진행 맞나요? 백업은?

### 3. 분배·로그인 동작 🔴

10. 실패 계정 "즉시 자동삭제" vs "Admin 확인 후 삭제"?(되돌리기 불가)
11. 분배=MOVE(보내면 Admin 화면 비워짐) — 이 모델 확정?
12. 분배 직후 "자동 전체 로그인" 자동 vs 운영자 버튼 수동 게이트?
13. 자동 로그인 대상 종토 확정 맞나요?
14. 보류·대기초과 재로그인은 "사람이 직접" 맞나요?

### 4. 동시 조작·상태 🟡

15. 현장 사람 + Admin 동시 조작 충돌 시 우선순위/락?
16. ROTATING 중 명령 거부 vs 큐잉?
17. 하위 재등록 시 옛 토큰 무효화?

### 5. 인증·계정 운영 🟡

18. 비밀번호 분실 대응(복구기능 없음 — 다른 운영자 재설정 vs SuperAdmin만)?
19. 가입 승인 "승인된 운영자 누구나" 맞나요?
20. SuperAdmin 기본 시드 `Superadmin`/`Superadmin` + 강제 변경, 보안 OK?

### 6. 범위·일정 🟡

21. MVP 범위(인증+모니터링+분배+보고까지, 전체 미러링 P5는 후순위) 맞나요?
22. 마감/일정 있나요?
23. 기존 앱 수정(auth/queue_runner/lib.rs에 훅·브리지 추가, 로직 무손상) 허용?

### 7. 순수 기술 선택 ⚪ (사수 위임 가능)

24. DB: SQLite vs Postgres?
25. 분배 계정 전달 형식: 임시 xlsx vs store 직삽 신규경로?
26. Rust 워크스페이스 전환 OK?

### 가장 중요한 3개 (당초 제안)

- #1·#2·#3(서버 위치·주소·TLS) — 없으면 한 줄도 못 짬
- #6·#7(서버에 비번 모으기 승인 + 키 보관) — 보안 직결
- #21(MVP 범위) — 일정/작업량 기준

---

## 5. 다섯 번째 — 사용자가 인프라 질문들의 의미를 모름 → 개념 설명

사용자 질문(요지): "서버를 어디 띄우나? / 고정 공인 IP가 회사 랜선 IP를 말하나? / TLS·자체서명·Let's Encrypt·사내 CA가 각각 뭐냐? / outbound 접속이 매크로 돌아가냐는 거냐? / Admin 웹 같이 서빙이 무슨 소리냐 — 랜선 꼽으면 고정 IP인데."

### Claude의 정정·설명

#### 먼저: SSE가 뭐냐

기존 매크로는 각 컴퓨터가 따로 도는 구조. Admin이 하위에 명령을 보내려면 둘 사이 **연결된 선**이 필요 = SSE.

- 하위가 Admin 서버에 **전화를 걸어 안 끊고 들고 있음**(SSE 연결).
- Admin은 그 **열린 전화기에 대고** "로그인 해"라고 말함 → 하위가 듣고 실행.
- 하위가 결과 보고는 전화 말고 **따로 문자(POST)**.
- 왜 하위가 거느냐: 하위는 IP가 계속 바뀌어 Admin이 먼저 못 찾아감 → **주소 안 바뀌는 Admin 쪽으로 하위가 항상 찾아가 전화**. (§3·§4)
- 결론: **"중앙 서버"라는 새 프로그램이 하나 떠 있어야 함.** 기존 매크로엔 없던 것.

#### ① 서버 위치 + ⑤ Admin 웹 서빙 = 사실상 같은 질문, 답 거의 정해짐

중앙 서버 프로그램을 **어느 컴퓨터에서 실행하냐**의 질문. 당신 환경이면 **회사 랜선 꽂힌 Admin 컴퓨터 한 대**가 웹 화면도, 하위 접속 서버도 겸하는 게 자연스러움. "별도 서버"=클라우드/타 컴퓨터(불필요).

#### ② 고정 공인 IP vs 도메인

- 공인 IP = 인터넷에서 그 컴퓨터를 가리키는 주소. **회사 랜선 IP가 그거 맞음.**
- 하위가 찾아가려면 이 주소를 앱에 박아둬야 함. 숫자 IP vs 도메인 선택.
- 함정: 회사 랜선이라고 항상 고정은 아님. "고정 IP 계약"만 안 바뀜. 바뀌면 하위 전부 길 잃음. 그래서 도메인이 안전(IP 바뀌어도 도메인만 가리킴 갱신).

#### ③ TLS / 인증서

- TLS = `https://`의 자물쇠. 오가는 데이터(ID/PW) 암호화. 안 쓰면 평문 노출 → 켜는 게 맞음.
- 인증서 = 서버 신분증. TLS 켜려면 필요.
  - 자체서명: 직접 만든 신분증. 공짜·즉시, 브라우저 경고. 내부용 그럭저럭.
  - Let's Encrypt: 무료 신뢰 인증서. **도메인 필요.**
  - 사내 CA: 회사 자체 발급 체계(큰 회사).

#### ④ outbound 접속 — 우선순위 오판 인정

매크로/로그인 얘기 아님. 하위가 서버에 거는 SSE 연결이 KT 등 테더링 망에서 막히냐는 것. 폰으로 웹서핑 되면 거의 100% 됨(둘 다 outbound) → **사수 질문 아님, 구현 후 테스트로 끝. 🔴 과했음, 제거.**

---

## 6. 여섯 번째 — 사용자 정정 + 파이프 구조·중앙 서버 프로그램 질문

사용자 핵심 발언:

- "인프라팀 없는 회사라 내가 다 개발한다. 그래서 묻는 거다."
- "SSE 구조면 **파이프가 2개**. 하나는 Admin이 만든 파이프, 다른 하나는 하위가 만든 파이프. IP 변동되면 하위 파이프가 일회용처럼 폐기되고 새 파이프 구축되는 거 아니냐?"
- "회사 랜선은 IP 안 바뀐다(바뀌려면 VPN 등 켜야 하는데 Admin은 안 켬). 기존 그대로 유지 + Admin은 웹으로 배포·추가."
- "그럼 Admin 실행하면 중앙서버 프로그램이 같이 실행되는 구조냐? 하위에서 이벤트 발생하면 중앙서버로 말해줘야 하는데, 이 중앙서버 프로그램에 기록되는 거냐?"

### Claude 답

#### 정정: 인프라팀 얘기는 헛다리

인프라팀 없고 직접 만든다 → TLS·인증서·서버 전부 **개발 결정**. 사수 질문에서 빼고 우리가 정할 기술 항목으로 이동. 사수엔 _업무/정책/운영_ 결정만 남김.

#### 파이프 2개 이해 — 맞음 (§3·§4-1)

- **파이프 ①** = 하위가 연 것(`GET /agent/stream`). 명령 받는 선. IP 바뀌면 끊김→새로 엶. 서버가 기기토큰으로 같은 하위 인식 → **죽은 파이프를 새 파이프로 갈아끼움**(라우팅 표). ← "일회용 폐기·재구축" 맞음.
- **파이프 ②** = Admin 브라우저가 연 것(`GET /admin/stream`). 상태·결과 실시간 받는 선. Admin 고정 IP라 안 끊김.
- 둘 다 클라이언트가 서버로 거는 것, 가운데 중앙 서버에 꽂힘.
- 주의: 명령은 파이프 ②로 안 감. Admin은 **POST**로 서버에 던지고 서버가 파이프 ①에 얹어 하위로 내려보냄. 파이프 ②는 받기 전용.
- 회사 IP 안 바뀜 확정 → **하위 앱에 IP 숫자 직접 박으면 됨, 도메인 안 사도 됨.** (②번 해결)

#### 핵심: 중앙 서버 프로그램 구조

배포 프로그램은 **딱 2종류**:

```
[회사 랜선 컴퓨터 (고정 IP)]
  ▶ 중앙 서버 프로그램 1개 (신규, Rust/axum)  ← 이거 하나만 띄움
     ├ 역할 A: 운영자에게 Admin 웹페이지(HTML/JS) 서빙
     ├ 역할 B: SSE 허브 (파이프 ①·② 둘 다 여기 꽂힘)
     └ 역할 C: DB (운영자·기기·토큰·감사로그·계정 스테이징)
  운영자는 브라우저로 서버 주소 접속 → Admin 화면이 뜸

[하위 컴퓨터 (폰 테더링, IP 바뀜)]
  ▶ 기존 pstmacro 데스크톱 앱 + 에이전트 레이어 (1개 프로그램)
```

**Q. Admin 실행하면 중앙서버가 같이 실행되냐?** → 거꾸로. "Admin"이라는 별도 실행 프로그램은 없음. 회사 컴퓨터에서 **실행하는 건 중앙 서버 한 개**, 그 서버가 운영자에게 Admin 웹화면을 브라우저로 내려줌. 운영자가 **브라우저로 서버 주소 열면 그게 Admin 실행**(웹사이트 원리). 이게 "기존 유지 + Admin은 웹 배포·추가"와 일치하고 제일 단순(서버 하나가 웹+API+SSE+DB 다 함).

**Q. 하위 이벤트가 중앙서버 프로그램에 기록되냐?** → 네. 하위가 일 생기면(로그인 결과·IP 변경·하트비트·분배 보고) **POST로 서버에 보냄**. 서버는 동시에 둘:

1. **DB에 기록**(감사로그 + 보고 사본; §7: 감사로그·기기·토큰은 서버 DB 원본).
2. **파이프 ②로 Admin 브라우저에 실시간 푸시**(운영자 즉시 확인).
   → 기록(DB) + 중계(화면) 둘 다. 단 §7대로 **쿠키·게시큐는 서버로 안 보내고 하위 로컬에만**; 서버엔 보고·감사 사본만. 계정 ID/PW는 분배 전 서버, 분배 후 하위로 MOVE.

---

## 7. 일곱 번째 — IP 하드코딩 여부 + Admin 페이지 IP/연결 확인 질문

사용자 질문: "회사 IP 안 바뀐다 → 하위 앱에 IP를 **하드코딩**해야 하나? 이 분야를 몰라서 묻는다. Admin 페이지엔 회사 IP를 넣는 구조나, 현재 랜선 IP가 뭔지·연결됐는지 확인하는 게 들어가야겠네?"

### Claude 답

#### 1. 하드코딩? → 아니, **설정값**으로

코드에 IP 박으면(하드코딩) 주소 바뀔 때 전 하위 재빌드·재설치해야 함 → 나쁨. 대신 **하위 앱에 "서버 주소" 입력칸(설정값)**. 설치 때 운영자가 1회 입력 → 앱이 파일에 저장(기존 `JsonStore` 재사용 가능).

```
[하위 앱 설정]
 서버 주소: [ http://123.45.67.89:8080 ]   ← 설치 때 1회
 기기코드:  [ 8237 ]                        ← 등록용(§6)
            [ 연결 ]
```

#### 2. Admin 페이지 — 직감 맞음, 방향만 정정

- **(A) "회사 IP를 넣는 구조"** → 거꾸로. 서버는 이미 그 컴퓨터에서 도니까 IP 입력 불필요. **서버가 자기 주소를 자동 표시**:
  ```
  ▶ 이 서버 주소: http://123.45.67.89:8080
    "각 하위 앱에 이 주소를 입력하세요"  [복사]
  ```
  운영자가 복사 → 하위 설정칸에 붙여넣음.
- **(B) "연결 확인"** → 정확히 맞음. 설계 §4 상태머신 + §10-3의 **하위 목록(연결 상태·IP 표시)**:
  ```
  ● 하위A  online  IP 1.2.3.4  방금 전
  ● 하위B  online  IP 5.6.7.8  3초 전
  ○ 하위C  오프라인 ―         꺼짐
  ```
  하위가 파이프 ① 열고 하트비트 보내면 online, IP 바뀌면 갱신, 끊기면 오프라인. 이미 설계에 있음.

#### 3. ⚠️ 직접 만들면 꼭 걸리는 함정 — 내부 IP vs 공인 IP

```
인터넷 ── [공유기/모뎀] ── 회사 컴퓨터들
            (공인 IP)        (각자 192.168.x.x = 내부 IP)
```

- **공인 IP** = 인터넷에서 보이는 주소(=회사 랜선 고정 IP). **하위가 입력할 진짜 주소.**
- **내부 IP**(192.168.x.x) = 회사 안에서만 통함. 하위(회사 밖 테더링)는 이걸로 못 찾음.
- 처리:
  1. 서버가 보여줄 주소는 **공인 IP**여야 함. 마침 폰 IP 알아낼 때 쓰는 `ipify`(`fetch_external_ip`, `adb.rs`)를 **재사용**해 서버 공인 IP 표시 가능.
  2. **포트포워딩**: 공유기 뒤에 서버 PC가 있으면, 밖→서버로 전달하라고 공유기에 1회 설정(예: "8080 → 192.168.0.5"). 안 하면 공인 IP로 와도 공유기에서 막힘.
  3. 공유기 없이 랜선이 공인 IP를 직접 받으면 2번 불필요.
- → **"회사 인터넷에 공유기가 있냐(=포트포워딩 필요하냐)" 한 번 확인** 필요.

**정리**: 하드코딩 ❌ → 하위 앱 설정칸 1회 입력 ✅ / Admin은 회사IP 입력받는 게 아니라 자동 표시 + 연결된 하위 목록·상태 ✅ / 함정: 하위 입력 주소는 공인 IP, 공유기 있으면 포트포워딩 1회 ⚠️.

---

## 8. 현재까지의 미해결·확인 대기 항목 (대화 종료 시점)

### 사용자가 확인해 줘야 할 환경 사실

- 회사 랜선이 **고정 공인 IP 계약**인지(사용자: "안 바뀌는 걸로 안다" — 잠정 확정).
- 회사 인터넷에 **공유기가 있는지**(→ 포트포워딩 필요 여부). **미확인.**

### 사수에게 받아야 할 정책 결정 (4번 질문지 중 정책성)

- 보안(서버에 비번 모으기 승인, 키 보관/회전, 평문 노출 vs 마스킹, 백업).
- 분배·삭제(즉시 자동삭제 vs 확인 후, MOVE 모델, 자동 vs 수동 로그인 게이트).
- 동시 조작 충돌 정책, ROTATING 중 명령 정책, 재등록 토큰 무효화.
- 비번 분실 대응, 가입 승인 범위, SuperAdmin 시드 보안.
- MVP 범위·일정, 기존 앱 수정 허용 범위.

### Claude가 추천+진행 가능한 기술 결정 (B 묶음)

- 워크스페이스 전환 / 서버 스택(axum+rusqlite+jsonwebtoken+argon2+aes-gcm 추천) / 로그인 상태 훅 노출 / 진행률 per-step화.

### 아직 안 한 정리거리

- `docs/설계.md:Zone.Identifier` 삭제 + `.gitignore`에 `*:Zone.Identifier` 추가(제안만, 미실행).
- C 미해결 5개를 설계서 §13에 정식 반영(제안만).
- B 추천안을 §11 로드맵에 반영(제안만).

### 다음에 풀기로 한 주제 (사용자 선택 대기)

- 기기코드 등록(§6) 실제 동작, 또는 서버 DB에 정확히 뭐가 어떻게 기록되는지(감사로그·§10-4 누적 보고), 또는 하위 에이전트 레이어가 기존 앱에 얹히는 방식.

---

# === 구현 단계 기록 (2026-06-28 ~ 06-30) ===

> 위까지는 **설계·계획 단계** 대화. 아래는 **실제 구현·연결 단계**. 사수 결정을 받아 Admin 웹 UI → 중앙 서버 → 하위 에이전트까지 코드로 구현하고 PR #324에 올린 전 과정을 빠짐없이 기록한다.

## 9. UI 미리보기 완성 + 설계서 결정 확정 (구현 전)

브랜치 `docs/admin-remote-control-design`(master에서 분기, 옛 `docs/admin-remote-control-plan` 초안은 폐기·삭제). Admin 웹 UI를 **별도 Vite 엔트리 `admin.html` + `src/admin/**`\*\*(Tauri 비의존, 브라우저로 바로 열림)로 9개 화면 미리보기 구현 후, 사용자 지시로 다음을 확정·반영:

- **로그인/비번변경 화면**: Enter만 쳐도 제출(`<form>`+`type=submit`), 강제 변경 시 현재 비번(`Superadmin`) 미리 채움.
- **통신 로그(§10-5)**: PowerShell식 콘솔. **통신 로그에 한해 ID/PW 평문 표시**(운영자 결정 — 다른 화면은 마스킹 유지). 함축 금지(계정별 결과·삭제 사유·IP 기존→바뀐 모두). `[REJECT]` 거부 로그 상세화(device_name·명령 한글·commandId(추적번호, 기기이름 아님)·사유+409·operator·src).
- **결과 보고(§10-4-2)**: 로그인 결과 + **게시 결과** 탭. 게시 성공=게시내용·링크, 실패=[자세히 보기] **백트레이스**(기존 `notifications.tsx`의 `SubLog`·`BatchItem`·`PostedContent` 모델 재사용, `util.rs`의 `backtrace_string`/`here!`/`transport_error_message!` 구조 그대로). 컴퓨터당 8곳 스크롤 + 성공/실패 배지 클릭 필터.
- **기기 연결**: [기기 삭제] 휴지통 버튼 추가(§6-4).
- **인증 방식 확정(§5)**: 운영자·기기 **모두 JWT**. 회수 = 운영자 **유저 버전 +1**(옛 토큰 일괄 무효), 기기 **기기표 줄 삭제**. 토큰 수명 = **운영자 8시간 / 기기 영구**(무만료). 비번 해시 argon2.
- **계정 암호화 확정(§7)**: **AES-256-GCM**(at-rest). 계정 ID/PW는 하위가 실제 로그인에 써야 해 복원 가능해야 하므로 암호화(해시 아님). at-rest=DB 저장 보호 / in-transit=TLS / 키는 환경변수(DB와 분리).
- **동시 조작 충돌(§4-3)**: 기본 Admin 우선, 단 현장 사람 행동 우선, **진행 중 작업(계정 단위)은 끊지 않고 완료 후 양보**(글 5개 게시 중이면 5개 다 올리고 → 사람 행동 → 다음 계정).
- **배포 토폴로지 부분확정(§2·§13)**: 중앙 서버 + Admin 웹을 회사 랜선 고정 IP 컴퓨터 1대에 함께. 스펙은 설계 무관이라 기록 안 함.
- **§13 결정 현황 A/B/C 요약** 신설(커밋 43c5ed5): A=사수 대기, B=구현 기본값 잠금(argon2·sqlx·키저장 환경변수), C=확정.

이 단계까지 PR #324 생성(설계서 + UI 미리보기). 리뷰어 pallas-dev 지정.

## 10. 사수 결정 (PR #324 코멘트, 2026-06-30)

> "DB는 PostgreSQL / 비밀번호 분실 복구 정책은 최상위 ADMIN이 변경가능하게 / 서버 주소·TLS·포트는 배포전 결정"

해석·반영:

- **DB = PostgreSQL** (SQLite 추천 폐기). `server/`는 sqlx + PostgreSQL.
- **비번 분실 = SuperAdmin이 하위 운영자 재설정**. 사용자 보충: **최상위 ADMIN은 비밀번호를 종이에 적어 보관**(복구 경로 없음), 그 외 하위 admin은 최상위 admin이 비번 변경 가능. → `POST /admin/operators/{id}/reset-password`(SuperAdmin 전용, 재설정 시 토큰버전 +1로 옛 토큰 무효) + 운영자 관리 화면에 [비번 재설정] 버튼(SuperAdmin에게 하위 운영자 행만 노출).
- **주소·TLS·포트 = 배포 전 결정**. `server/src/config.rs`에 자리만 두고 값 비움(주석 `배포 전 결정`).

§13에서 A1·A2를 "확정"으로 이동, 설계서 §5·§6·§7 반영.

## 11. 중앙 서버 구현 (`server/`, 신규 독립 Rust 크레이트)

Tauri 앱(`src-tauri`)과 **워크스페이스로 묶지 않은 독립 크레이트** → 기존 빌드 무손상. (cargo가 axum 등 fetch 가능함을 먼저 확인.)

- 스택: **axum 0.7 + sqlx 0.8(PostgreSQL, 런타임 쿼리 API라 빌드 시 DB 불필요) + jsonwebtoken 9 + argon2 0.5 + aes-gcm 0.10 + tokio + tower-http(cors)**.
- 모듈: `config`(환경변수+배포전 결정 자리), `crypto`(argon2 해시·AES-256-GCM 암복호·기기코드), `jwt`(운영자 8h+ver / 기기 무만료), `model`(도메인+DTO, camelCase 직렬화), `distribute`(균등+랜덤 분배), `repo/`(트레잇 + **in-memory**[개발·오프라인·테스트] + **postgres**[운영]), `hub`(SSE 허브: device_id→스트림 라우팅 + Admin 브로드캐스트), `state`(인증/감사 헬퍼), `routes`(§10 엔드포인트 전부), `error`(→HTTP), `main`(부트스트랩+SuperAdmin 시드).
- `migrations/0001_init.sql`(operators/devices/device_codes/staged_accounts/audit_log). `PostgresRepo::connect`가 기동 시 멱등 생성.
- **DATABASE_URL 없으면 in-memory**로 동작(개발/미리보기), 있으면 PostgreSQL.
- 보안: 계정 PW는 AES 암호문으로만 저장(목록 응답에 PW 미반환), SuperAdmin 시드(`Superadmin`/`Superadmin`→강제 비번변경), 운영자 삭제·비번재설정은 SuperAdmin 전용, Superadmin 계정 삭제 불가.
- 검증: `cargo test` 9개(crypto AES 왕복·argon2·기기코드, JWT 운영자/기기, 분배 partition/balance) 통과. **실서버 부팅 후 HTTP 스모크**: 로그인→JWT, 잘못된 비번 401, 기기코드 발급→등록(1회용 재사용 400), import→distribute(MOVE, 잔량 감소 확인), signup→approve→reset-password→새 비번 로그인 200, 운영자 삭제, **Superadmin 삭제 403**, 감사로그 적재 모두 ✅.

## 12. Admin UI ↔ 서버 연결 (`src/admin/api.ts` + 9개 화면)

- `api.ts`: HTTP 클라이언트(토큰 localStorage, `fetch`). **서버 미연결 시 `OfflineError`→각 화면이 기존 더미로 폴백**(오프라인 미리보기 무손상). 서버 주소 `VITE_ADMIN_API`(기본 localhost:8080).
- 연결된 화면: 로그인·회원가입·강제/일반 비번변경, 운영자 관리(승인/거절/삭제/**비번재설정**), 기기 연결(코드 발급·목록 폴링·삭제), 계정 분배(스테이징 목록·계정 추가 import·분배), 통신 로그(감사로그 폴링).
- **운영자 관리에 [비번 재설정] 버튼 신설**(사수 결정, SuperAdmin 전용·하위 운영자 행).
- 결과 보고의 **게시 결과**는 하위 에이전트가 보고할 데이터라 더미 유지(아래 §16).
- 검증: `pnpm typecheck` 0, `eslint` 0.

## 13. 자체 Adversarial 리뷰 (workflow) → 머지 전 보강

3개 병렬 리뷰어(보안/설계충실/UI보존)로 점검 → 발견·수정:

- **(HIGH)** `admin_stream` SSE가 토큰버전·존재·승인 재검증 누락 → 삭제·강제로그아웃된 운영자가 평문계정 스트림 계속 수신 가능 → `auth_operator_token` 공유 헬퍼로 재검증 추가.
- **(HIGH)** 운영모드(DATABASE_URL 설정)에서 기본 JWT 비밀/암호화 키가 그대로면 위조·복호화 위험 → **fail-closed**(기동 거부) + 개발모드 경고.
- **(MEDIUM)** 로그인 계정 열거(타이밍) → 없는 아이디여도 더미 해시로 argon2 1회(상수시간). signup 409 노출 → 균일 응답.
- **(MEDIUM)** distribute가 MOVE를 배달 확인 전에 수행 + §4-2 online 게이트 없음 → **MOVE 전 대상 online 검증**(아니면 409 [REJECT], 제거 안 함).
- **(MEDIUM)** 인앱 비번변경 화면 `go` 누락으로 변경 후 네비게이션 안 됨 → `AppScreen`에 `go` 전달.

## 14. 하위 에이전트 레이어 (`src-tauri/src/agent/`, 기존 코드 무손상·추가만)

> 사용자 정정: "기존 무손상 = 수정 금지"가 아니라 **신호·레이어 추가는 OK**. 같은 크레이트라 기존 `pub(crate)` 함수를 호출만 하면 됨.

- 신규 모듈 `agent/{config,net,mod}.rs`:
  - `config`: 서버주소+기기토큰을 앱 데이터 루트(`auth::app_data_root` 재사용)에 `agent.json`으로 영속.
  - `net`: 등록/하트비트/상태/결과 POST + SSE 열기(reqwest 비동기 `chunk()`로 읽어 추가 feature 불필요).
  - `mod`: SSE 명령 수신 루프 + 백오프 재연결, 명령 디스패치, 하트비트 루프, 상태보고 루프, Tauri 명령 3개.
- 명령 처리: `distribute_accounts`(받은 계정을 기존 `JsonStore<Account>`에 등록 + 기존 종토 선택로그인 경로(`buildLoginNowItem` 미러: QueueNowItem.plan.login)로 enqueue + `start_if_idle`), `import_then_login_all`, `delete_accounts`.
- **§10-4 로그인 결과 보고 + 실패 자동삭제 + 누적**: enqueue한 큐 아이템이 Done 될 때까지 폴링 → 계정 상태로 **성공(Active)/보류(OnHold)/대기초과(TimedOut)/실패(BadCredentials·Challenge·Blocked·Error)** 4분류 → 같은 commandId로 서버 보고(통신로그에 ID/PW 평문) → **실패 자동삭제**(보고 먼저, 되돌리기 불가) → 누적 ledger(`agent-ledger.json`) 갱신.
- **ROTATING 상태 신호(추가만)**: `auth/adb.rs::toggle_airplane_mode`에 `crate::agent::report_state_change` **2줄 추가**(회전 시작=`rotating`, 복구=`online`+바뀐 IP). adb 로직 무변경, 미등록이면 no-op. 전역 채널→에이전트 상태보고 루프가 `/agent/state`·하트비트로 전달.
- `lib.rs`: `mod agent` + 명령 3개(agent_register/status/unregister) 등록 + setup에서 에이전트 루프 spawn(추가 1줄).
- **하위 등록 화면**(§6-2): `src/features/device-register/` 신규 + nav '원격제어' 항목 + `ipc.agent`. 사람이 입력하는 건 서버주소+기기코드 둘뿐, 토큰은 자동 발급·저장.
- 검증: Tauri lib `cargo build` 무경고(병합된 master 기준), 프론트 typecheck/eslint 0, 213 테스트 통과. **서버↔에이전트 SSE 명령전달 실측**: 등록→SSE 구독→Admin distribute→하위가 `data: {accounts,commandId,type:"distribute_accounts"}` 수신 확인(에이전트 파서가 먹는 그 형식).

## 15. 최신 master 병합 정합성

구현 도중 사수가 최신 master(#341 종토 재시도 표시·#342 종토 500 빨리 실패 등)를 브랜치에 병합(`443450f`). 병합 후 에이전트 코드가 그대로 컴파일됨을 재확인(Tauri lib 0, 프론트 0) — 충돌·불일치 없음.

## 16. 현재 상태 (2026-06-30 기준)

**커밋(브랜치 `docs/admin-remote-control-design`, PR #324 OPEN)**:

- `43c5ed5` §13 A/B/C 결정 요약 등 설계 정리
- `94032e4` 중앙 서버 구현 + Admin UI 백엔드 연결
- `f494c9e` 하위 에이전트 레이어 + 등록 화면
- `443450f` (사수) master 병합
- `e098f0a` §10-4 결과 보고+자동삭제+누적 + ROTATING 신호

**✅ 완료·검증**: 중앙 서버(인증·운영자·기기·계정 스테이징/분배 MOVE·통신로그·SSE 허브) / Admin 웹 9화면 ↔ 서버 연결(더미 폴백) / 하위 에이전트(등록·SSE 수신·계정등록·로그인 enqueue·§10-4 보고·실패 자동삭제·누적·하트비트·ROTATING 신호·재연결) / 하위 등록 화면.

**⏳ 미구현 + 이유**:

- **게시(posting) 자체 결과·백트레이스를 §10-4-2 구조로 서버 보고하는 배선** — 로그인 결과(§10-4)는 보고하지만 게시 결과는 별도 흐름이라 결과보고 화면 '게시 결과' 탭은 현재 더미.
- §10 `/agent/accounts`·`/devices/{id}/files`(파일 전달) — 분배가 SSE 명령에 계정을 실어 보내 현재 불필요해 생략.
- **실제 네이버 로그인 실행 검증**은 폰·ADB·웹뷰가 필요 → 사수가 직접 검증(코드는 컴파일·프로토콜 실측까지 책임).
- 배포(주소·TLS·포트)는 사수 "배포 전 결정"으로 config 자리만 비움.

**검증 합계**: server `cargo test` 9개 + 실HTTP 스모크 / 에이전트 SSE 실측 / Tauri lib cargo build 무경고 / 프론트 typecheck·eslint 0 / 213 vitest 통과.
