# PR #384 — fix(like): 차단/만료를 form 원문으로 판정 + 상태 반영(loginId 매칭)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/384
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-03T02:52:33Z
- 병합: 2026-07-03T02:52:44Z
- 브랜치: `fix/like-restriction-raw-probe` → `master`

## PR 본문 원문

## 두 가지 수정

**1. 상태가 안 바뀌던 회귀 (핵심)**
좋아요는 프론트가 **loginId**(쿠키 키, `like-modal.tsx §85`)로 넘기는데, #383이 계정 상태를 **id로
매칭**(`apply_status_by_id`)해서 안 맞아 상태가 저장되지 않았습니다. → **`apply_status_by_login_id`**
로 매칭. 이제 Relogin/Blocked가 실제로 저장되고, 계정관리 화면 재조회 시 배지에 반영됩니다.

**2. 추측 대신 원문 판정 (사용자 요청)**
좋아요가 막히면 `probe_restriction_raw`가 `/api/community/profile/users/form` 응답 **status+body를
자르지 않고 통째로** 로그에 남기고, 그 **원문으로** 차단/만료를 확정합니다:
- 차단: `UMON_*_BANNED`/`403F0x`/`penaltyDays`/`아이디 잠금`/`보호조치`/`이용제한`/`글쓰기 금지`/`비정상적인 활동`
- 만료: 로그인 페이지(`nidlogin.login`/`frmNIDLogin`)·비로그인 응답
- getProfile 로그아웃·좋아요 실패 모두 이 프로브를 거쳐 판정 → 다음 실행 로그엔 판별 근거 원문이 그대로 남습니다.

## 검증
- `cargo test --lib` 1002 pass. 게시 경로 무손대.
- ⚠️ 실계정 확인 필요: 차단 계정=비활성 배지+쿠키만료 사라짐 / 만료=재로그인 배지. form 원문이 로그에 통째로 남음.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## 변경 파일 전체

- `src-tauri/src/lib.rs` — modified, +8/-6
- `src-tauri/src/naver_automation/like_flow.rs` — modified, +63/-99
- `src-tauri/src/naver_automation/packet_client.rs` — modified, +87/-0

## 커밋 전체

### 65f6cb601e56c61a11466d32fba9798519c92d0b

작성자: vscodereader

fix(like): 차단/만료를 form 원문으로 판정 + 상태 반영(loginId 매칭)

- 상태 안 바뀌던 회귀 수정: 좋아요는 loginId로 넘어오는데 #383이
  id로 매칭해 안 맞았다. apply_status_by_login_id로 매칭.
- probe_restriction_raw: /profile/users/form 응답 status+body를
  자르지 않고 통째로 로그에 남기고 원문으로 판정.
- 차단 신호: UMON밴/403F0x/penaltyDays/아이디잠금/보호조치/
  이용제한/글쓰기금지/비정상적인활동. 만료: 로그인페이지/비로그인.
- getProfile 로그아웃·좋아요 실패 모두 이 프로브로 판정.
- 게시 경로 무손대. cargo test 1002 pass.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문


