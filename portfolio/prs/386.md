# PR #386 — feat(admin): 게시 명령 화면 UI (종토 확정, 카페/블로그/밴드 버튼만)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/386
- 작성자: vscodereader
- 상태: CLOSED
- 작성: 2026-07-03T03:35:57Z
- 병합: 미병합
- 브랜치: `feat/admin-publish-command` → `docs/admin-remote-control-design`

## PR 본문 원문

설계서 07(#379)의 **게시 명령 화면 UI**를 구현합니다(1단계: UI 먼저). 베이스는 `docs/admin-remote-control-design`(#324) — Admin UI가 그 위에만 있습니다.

## 만든 것
- **사이드바 '계정 분배' 아래 '게시 명령'** 화면 추가(`src/admin/features/publish-command/`).
- **① 하위 카드 4열 그리드 + 글 선택**: 카드 클릭 → 파랑 강조 + 아래 글 셀렉트 활성, 글 라벨 6자 말줄임.
- **② 대상 4버튼**: 종목토론방(활성) / 카페·블로그·밴드(‘추후’ 배지·비활성).
- **③ (종토) 하위별 독립 패널**: 카테고리 6버튼 → 시장 3버튼(토론=전체 고정) → 종목수 N → 성공계정 선택. **하위끼리 절대 안 섞임.**
- **④ 게시하기**: 선택 하위마다 요약(실제 `publish_posts` 서버 배선은 후속).
- **종목 선택 알고리즘**(신규 순수함수 `stock-select.ts`): 불꽃🔥 우선 + 상위 N + **삼성전자/하이닉스 이름 제외**. 단위테스트 4케이스(정상/불꽃초과/N초과/제외).

## 결정 반영
- 제외 = "삼성전자"·"하이닉스" 이름만(사용자 확정). 종토만. 종목선택은 UI에서 미리보기 계산(하위/서버 배선은 후속).

## 미배선(후속)
서버에 아직 없는 데이터(하위 글목록·성공계정·종목 미리보기, 설계 §8)는 Admin 기존 **오프라인 더미 폴백** 패턴으로 채워 화면을 완성했습니다. 실데이터·`publish_posts` 명령·서버 프록시는 다음 단계.

## 검증
`pnpm typecheck` · `eslint` · `vitest`(신규 10개 통과, 전체 376). 미리보기: `pnpm dev` → `/admin.html` → 좌측 '게시 명령'.

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## 변경 파일 전체

- `src/admin/admin-app.tsx` — modified, +4/-0
- `src/admin/features/publish-command/publish-command.test.tsx` — added, +27/-0
- `src/admin/features/publish-command/publish-command.tsx` — added, +595/-0
- `src/admin/features/publish-command/stock-select.test.ts` — added, +89/-0
- `src/admin/features/publish-command/stock-select.ts` — added, +79/-0
- `src/admin/screens.ts` — modified, +2/-0

## 커밋 전체

### c10a05cef111bb406fa7b7c9f6881624f3733ac6

작성자: vscodereader

feat(admin): 게시 명령 화면 UI (종토 확정, 카페/블로그/밴드 버튼만)

설계서 07(#379) 구현 1단계 — UI 먼저. 사이드바 '계정 분배' 아래 '게시 명령' 추가.

- ① 하위 카드 4열 그리드 + 글 선택(카드 클릭=파랑 강조, 셀렉트 활성, 6자 말줄임)
- ② 대상 4버튼(종토만 활성, 카페/블로그/밴드는 '추후')
- ③ 종토: 하위별 독립 패널 — 카테고리6/시장3(토론=전체고정)/종목수N/성공계정
- ④ 게시하기(선택 하위마다 요약; publish_posts 서버배선은 후속)
- 종목선택 알고리즘(불꽃 우선+상위N+삼성전자/하이닉스 제외) 순수함수+테스트 4케이스
- 서버 미배선 데이터(글목록·성공계정·종목)는 Admin 기존 오프라인 더미 패턴으로 폴백

베이스: docs/admin-remote-control-design(#324) — Admin UI가 그 위에만 있음.
tsc·eslint·vitest(신규 10) 통과.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### 51939ae8ea46fbc77cd19ac7bbc868129dcf3f2f

작성자: vscodereader

feat(admin): 게시 버튼을 하위 패널로 이동 + 4버튼(즉시/예약/나눠서) 적용

- 맨 아래 단일 '게시하기' 버튼 제거 → **각 하위 패널의 제외 안내 밑**으로 이동.
  하위마다 옵션이 다르므로 패널별로 독립 발행(대원칙 0-1).
- 데스크톱 pstmacro와 동일한 **4버튼** 적용(#267-5): 지금 바로 게시 / 예약 게시 /
  나눠서 즉시 게시하기 / 나눠서 게시 예약하기.
- 나눠서 활성 조건도 동일: 계정 2개↑ + 종목 2개↑ + 종목수 ≥ 계정수. 균등 분배
  미리보기(distributeEvenly, 14·4계정→4·4·3·3) 토스트로 표시.
- distributeEvenly 순수함수 + 테스트 추가(데스크톱 distributeStocksEvenly 미러).

tsc·eslint·vitest(신규 12) 통과.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### 84e17e8736fd14b3c5de287110365cc0dc6880be

작성자: vscodereader

feat(admin): 게시 대상을 하위별로 — 하위마다 종토/카페 따로 선택

전역 게시 대상 버튼(②)을 제거하고, 하위 선택+글 선택 후 **하위마다** 대상
(종토/카페/블로그/밴드)을 고르게 한다. 한 대는 종토, 다른 대는 카페처럼
서로 다른 대상을 가질 수 있다(대원칙 0-1: 하위끼리 안 섞임).

- 상태 target(전역) → targetByDev(하위별).
- 신규 DeviceBlock: 기기 헤더 + 선택 글 배지 + 게시 대상 4버튼(글 선택돼야
  활성, 카페/블로그/밴드는 추후 비활성) + 대상별 상세.
- ForumPanel → ForumConfig로 분리(외곽 Paper·기기헤더는 DeviceBlock이 제공).
  종토면 카테고리/시장/종목수/계정 + 4버튼, 그 외 대상은 '추후 구현' 안내.

tsc·eslint·vitest(12) 통과.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### d3d3810742c688afbe1113c7b22a79a79ae05589

작성자: vscodereader

style(admin): 게시 4버튼 한 줄 배치 + 라벨 축약(지금/예약/나눠서 즉시·예약)

한 줄 4버튼(Group grow, size xs)으로 좁히고 라벨을 짧게. 동작 동일.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문


