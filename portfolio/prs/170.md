# PR #170 — fix(posts): 종목 선택 체크박스 직접 클릭이 즉시 반영되도록

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/170
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-06-09T05:33:28Z
- 병합: 2026-06-09T07:29:44Z
- 브랜치: `fix/stock-checkbox` → `master`

## PR 본문 원문

## Summary

- 종목 선택 모달에서 체크박스를 직접 클릭하면 체크표시가 한 박자 늦게 떴다(종목 하나 선택 → 표시 없음, 다음 종목 선택 → 그제서야 이전 종목에 체크). 선택을 해제해도 체크가 사라지지 않았다.
- `stock-crawl-modal.tsx`의 `<Checkbox checked readOnly>`가 원인(onChange 없는 controlled checkbox → React가 상태를 한 렌더 늦게 동기화). **#165(계정 체크박스)와 동일 근본원인.** onChange 부여(`readOnly` 제거) + `stopPropagation`(이중 토글 방지)으로 즉시 반영되게 한다. 행 클릭 토글은 불변(additive).

Closes #172

## 변경 범위

**수정 (additive)**
- `src/features/posts/stock-crawl-modal.tsx` — 종목 행 체크박스에 `onChange` + `onClick stopPropagation`(readOnly 제거).
- `src/features/posts/stock-crawl-modal.test.tsx` — 체크박스 직접 클릭 단일 토글 검증 테스트 추가.

## Test plan

로컬 검증(브랜치 `fix/stock-checkbox` HEAD):

- [x] `pnpm typecheck` — clean
- [x] `pnpm exec vitest run stock-crawl-modal.test.tsx` — **14 passed**
- [x] pre-push `vitest run`(전체 프론트) 통과
- [x] 윈도우 통합 빌드(test/win-build)에서 즉시 체크/해제 동작 확인
- [ ] CI green (frontend + rust + tauri-build)

## 변경 파일 전체

- `src/features/posts/stock-crawl-modal.test.tsx` — modified, +13/-0
- `src/features/posts/stock-crawl-modal.tsx` — modified, +6/-1

## 커밋 전체

### 3e3c94ed277ad50d92f77c601d8bf7dcefeec384

작성자: vscodereader

fix(posts): 종목 선택 체크박스 직접 클릭이 즉시 반영되도록

stock-crawl-modal의 체크박스가 `<Checkbox checked readOnly>` — onChange가
없는 controlled checkbox라 React가 체크 상태를 한 렌더 늦게 반영했다(선택해도
다음 항목을 눌러야 이전 체크가 뜨고, 해제해도 체크가 안 사라짐). #165(계정
선택)와 동일 근본원인. onChange를 부여하고(readOnly 제거) 클릭이 행으로
버블링돼 이중 토글되지 않도록 stopPropagation을 추가한다. 행 클릭 토글은 유지.

- 체크박스 직접 클릭 시 단일 토글 검증 테스트 추가.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문


