# PR #320 — fix(auth): [진단] 로그인 게이트 OPEN 순간 네트워크/안티봇 스냅샷 로그

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/320
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-06-29T06:19:13Z
- 병합: 2026-06-29T06:19:21Z
- 브랜치: `fix/login-gate-network-snapshot-log` → `master`

## PR 본문 원문

## 목적
캡차 재발 원인이 **"로딩바가 도는 중(=네트워크 아직 활발)에 게이트가 열려 일찍 타이핑"** 인지 **실측 증거**로 확정하기 위한 진단 로그. (사수 의도: DOM 로딩이 모두 완료되면 입력)

## 변경
- `GATE_NET_SNAPSHOT_JS` — `performance` 리소스 타이밍만 읽는 **순수 관측**(전역 미변경 → 봇탐지 표면 0):
  - `rs` readyState / `resCount` 완료 리소스 수
  - `sinceLastNetMs` 마지막 완료 리소스 이후 경과(ms) — **작을수록 아직 로딩 중**
  - `antibot` 도착한 `default_ecc`·`wtm`·`ncaptcha`·`nclk` + 각 시각
  - `iframes` iframe 개수
- `wait_for_login_form` 게이트 통과 직전 `[LOGIN] ⏱ 게이트 OPEN 직전 네트워크 스냅샷: {...}` 로깅

## 안전성
**순수 진단** — CDP 추가 enable 없음(`enable_page_only` 유지), 입력/동작 경로 무변경. 동작 변화 0.

## 검증
- `cargo build --lib` OK
- `cargo test --lib auth::login_flow` → 54 passed

## 다음
스냅샷에서 `sinceLastNetMs`가 작거나 `antibot`에 `default_ecc`/`wtm` 누락이 보이면 → Route A(network-idle 게이트)로 본수정.

## 변경 파일 전체

- `src-tauri/src/auth/login_flow.rs` — modified, +31/-0

## 커밋 전체

### 5470ee8d1585a4d87495e9a74b7ccf57326638d7

작성자: vscodereader

fix(auth): [진단] 로그인 게이트 OPEN 순간 네트워크/안티봇 스냅샷 로그 추가

사수 의도("DOM 로딩이 모두 완료되면 입력")대로 게이트가 동작하는지 실측하기 위해,
wait_for_login_form 이 통과(타이핑 직전)하는 바로 그 순간의 상태를 stderr/파일 로그로
한 줄 남긴다. 캡차 재발이 "로딩바 도는 중(네트워크 활발)에 게이트가 열려 일찍 타이핑"
때문인지 증거로 확정하기 위함.

- GATE_NET_SNAPSHOT_JS: performance 리소스 타이밍만 읽는 순수 관측(전역 미변경 → 봇탐지
  표면 0). rs(readyState)/resCount/sinceLastNetMs(마지막 완료 리소스 후 경과)/antibot(도착한
  default_ecc·wtm·ncaptcha·nclk + 시각)/iframes 를 JSON 한 줄로 반환
- 게이트 통과 직전 "[LOGIN] ⏱ 게이트 OPEN 직전 네트워크 스냅샷: {...}" 로깅
- CDP 추가 enable 없음(enable_page_only 유지), 동작/입력 경로 무변경 — 순수 진단

sinceLastNetMs 가 작으면(예 <300ms) 방금도 리소스가 끝났다 = 아직 로딩 중인데 타이핑이
나간다는 직접 증거. 증거 확인 후 Route A(network-idle 게이트)로 본수정 예정.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문


