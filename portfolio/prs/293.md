# PR #293 — feat(accounts): 네이버블로그도 선택 로그인 지원 (종토방 흐름 재사용)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/293
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-06-25T01:05:20Z
- 병합: 2026-06-25T01:05:30Z
- 브랜치: `feat/blog-selective-login` → `master`

## PR 본문 원문

## 요구
네이버블로그도 종목토론방처럼 — 계정 추가 → 체크박스 선택 → '선택 로그인' 버튼 활성화 →
누르면 로그인.

## 변경 (백엔드 변경 없음)
블로그는 네이버 쿠키 기반이라 `buildLoginNowItem`이 이미 naver 로그인으로 묶는다(band만 별도).
막고 있던 건 프론트 게이트(`forumOnly`, 전부 forum일 때만 버튼 노출) 하나뿐.

- `isSelectiveLoginPlatform(forum|blog)` 순수 헬퍼 추가(login-queue.ts).
- accounts.tsx 게이트를 forum 전용 → forum/blog로 확장(`forumOnly` → `loginEligible`).
- 카페(naver)·밴드(band)는 기존대로 제외 — 게시 직전 백엔드가 로그인을 원자 처리.

## 테스트
- `isSelectiveLoginPlatform`: forum/blog=true, naver/band=false.
- `buildLoginNowItem`: 블로그 계정 → platform "naver" 로그인 + force/useAdb, locs는 blog 표시.
- vitest login-queue + accounts 41건 통과, tsc 0.

## 변경 파일 전체

- `src/features/accounts/accounts.test.tsx` — modified, +4/-3
- `src/features/accounts/accounts.tsx` — modified, +9/-8
- `src/features/accounts/login-queue.test.ts` — modified, +28/-1
- `src/features/accounts/login-queue.ts` — modified, +11/-1

## 커밋 전체

### 8bbc725a1e5648b7bb16bdcfc6af88f1e69eb81e

작성자: vscodereader

feat(accounts): 네이버블로그도 선택 로그인 지원 (종토방 흐름 재사용)

종목토론방처럼, 블로그 계정을 체크박스로 선택하면 '선택 로그인' 버튼이
활성화되고 누르면 로그인되게 한다. 블로그는 네이버 쿠키 기반이라
buildLoginNowItem이 이미 naver 로그인으로 묶으므로 백엔드 변경은 없다.

- isSelectiveLoginPlatform(forum|blog) 순수 헬퍼 추가(login-queue.ts).
- accounts.tsx의 선택 로그인 게이트를 forum 전용 → forum/blog로 확장
  (forumOnly → loginEligible). 카페·밴드는 기존대로 제외(게시 직전 원자 로그인).

테스트: isSelectiveLoginPlatform 게이트 + 블로그→naver 로그인 매핑 단위 테스트.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문


