# PR #434 — fix(clip): 프로필 생성을 죽은 graphql → creatorhub REST로 교체 (404 수정)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/434
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-15T04:12:52Z
- 병합: 2026-07-15T04:21:44Z
- 브랜치: `fix/clip-profile-creatorhub-rest` → `master`

## PR 본문 원문

## 문제
클립 댓글 전 계정마다 하는 "프로필 생성"이 `POST clip.naver.com/api/graphql`(NaverProfile/SignUp)을 호출했는데, 네이버가 그 엔드포인트를 폐기해 **HTTP 404** → 프로필 미생성 → 댓글 실패(`클립 graphql 요청이 실패했습니다(HTTP status 404)`).

## 근거 (실측 패킷)
`네이버 클립 프로필.pcapng`(2026-07-15) 복호화로 현재 실브라우저 경로 확정:
| 단계 | 엔드포인트 |
|---|---|
| 존재확인 | `GET creatorhub-api/api/v1.0/clip/profiles` (이미 맞음) |
| 기본 프로필 | `GET creatorhub-api/api/v6.0/clip/profiles/naver-profile` |
| 생성 | `POST creatorhub-api/api/v5.0/clip/profiles` 본문 `{clipId,nickname,profileImageUrl}` |

- 성공 판정: `header.code==0`, 본인인증 미완료: `-7020`
- signup 컨텍스트 헤더: `Origin=clip.naver.com`, `Referer=…/signup?version=light`, `x-creator-hub-sid: clip`

## 변경
- `fetch_naver_profile`: graphql NaverProfile → creatorhub v6.0 naver-profile GET
- `sign_up`: graphql SignUp → creatorhub v5.0 profiles POST
- `parse_sign_up`(graphql `__typename`) → `parse_profile_result`(creatorhub `header.code`)
- `creatorhub_signup_headers()` 신설, clip_base/graphql 경로 제거
- clipId 생성·safe_nickname·존재확인(v1.0)은 이미 맞아 유지

## 검증
- naver_clip **31개 테스트 통과**, 전체 lib 빌드 경고·에러 0

## ⚠️ 후속(정직)
이 PR은 **프로필 생성** 404를 고침. 그다음 단계인 **창작자 미디어목록 조회**(`clip_list.rs`)는 아직 `clip.naver.com/api/graphql`을 쓰는데, 프로필 생성에서 막혀 거기까진 도달 못 해 검증 못 함 — 실기기 재현 시 그 단계도 404면 같은 방식으로 패킷 대조해 고칠 것.

## 변경 파일 전체

- `src-tauri/src/naver_clip/headers.rs` — modified, +33/-0
- `src-tauri/src/naver_clip/profile.rs` — modified, +146/-128

## 커밋 전체

### 56296c88deb6226c44bef7f0fc7b83d8d2cb383f

작성자: vscodereader

fix(clip): 프로필 생성을 죽은 graphql → creatorhub REST로 교체(404 수정)

클립 댓글 전 프로필 생성이 POST clip.naver.com/api/graphql(NaverProfile/SignUp)을
호출했으나 네이버가 그 엔드포인트를 폐기해 HTTP 404 → 프로필 미생성 → 댓글 실패.

실측 패킷(네이버 클립 프로필.pcapng, 2026-07-15)으로 현재 경로 확정:
- 기본 프로필: GET creatorhub-api/api/v6.0/clip/profiles/naver-profile
- 생성:        POST creatorhub-api/api/v5.0/clip/profiles (본문 {clipId,nickname,profileImageUrl})
- 성공판정: header.code==0, 본인인증 미완료: -7020
- signup 컨텍스트 헤더(Origin=clip.naver.com, Referer=signup, x-creator-hub-sid: clip)

존재확인(v1.0)·clipId 생성·safe_nickname은 이미 맞아 유지. 31개 테스트 통과.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문


