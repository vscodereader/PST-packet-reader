# PR #440 — feat(admin): 통신 로그 일자별 필터 — 3단 종속 목록형(컴퓨터→날짜)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/440
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-20T01:06:27Z
- 병합: 2026-07-20T01:33:02Z
- 브랜치: `feat/439` → `master`

## PR 본문 원문

## Summary

- 통신 로그(comm-log) 필터를 **1단(전체/특정 하위) → 3단 종속 드롭다운**으로 확장해 **일자별로** 골라 볼 수 있게 함.
- 서버 감사로그 `ts`가 UTC ISO라 날짜 추출·표시를 **KST(UTC+9)** 로 변환(서버 결과보고 일자집계와 동일 기준 → 자정 근처 로그 날짜 밀림 방지).

### 동작
- **목록1(컴퓨터)**: `Admin(전체)` · 하위COM들
- **목록2**: 하위COM 선택 시 그 COM이 **기록을 가진 날짜만**(없는 날짜 제외) / Admin 선택 시 컴퓨터 목록(시스템·하위COM)
- **목록3**: 평소 비활성 → `Admin + 하위COM` 선택 시에만 활성(그 COM의 날짜)
- 종속 초기화(목록1 변경→2·3 리셋, 목록2 변경→3 리셋), [내보내기]는 현재 필터 로그만 저장

### 변경
- `src/admin/features/comm-log/comm-log-filter.ts` (신규, 순수 로직) + `comm-log-filter.test.ts` (24 tests)
- `src/admin/features/comm-log/comm-log.tsx` (드롭다운 3개 + KST 시각 표시)
- `docs/Admin UI/06-통신로그.md` (§일자별 필터 설계 추가)

Closes #439

## Test plan
- [x] `pnpm typecheck` · `pnpm lint` · `pnpm format:check`
- [x] `pnpm test` — 43 files / 524 tests pass (신규 comm-log-filter 24 포함)
- [ ] 실서버/실기기: 여러 하위·여러 날짜 로그에서 3단 필터·KST 날짜 확인 (사수/사용자)

> ⚠️ CI는 결제 보호로 미실행(`[skip ci]`). 머지는 사용자 지시 대기.

## 변경 파일 전체

- `docs/Admin UI/06-통신로그.md` — modified, +39/-2
- `src/admin/features/comm-log/comm-log-filter.test.ts` — added, +182/-0
- `src/admin/features/comm-log/comm-log-filter.ts` — added, +156/-0
- `src/admin/features/comm-log/comm-log.tsx` — modified, +123/-14

## 커밋 전체

### b9a6e80ed094955242d0bcece7039d9f5e1299ef

작성자: vscodereader

feat(admin): 통신 로그 일자별 필터 — 3단 종속 목록형(컴퓨터→날짜) [skip ci]

통신 로그 필터를 1단(전체/특정 하위)에서 3단 종속 드롭다운으로 확장.

- 목록1(컴퓨터): Admin(전체) / 하위COM들
- 목록2: 하위COM 선택 시 그 COM이 기록을 가진 날짜만(없는 날짜 제외);
  Admin 선택 시 컴퓨터 목록(시스템/하위COM)
- 목록3: 평소 비활성, Admin+하위COM 선택 시에만 활성 → 그 COM의 날짜
- 날짜 추출·표시 모두 KST(UTC+9)로 변환(서버 결과보고 일자집계와 동일 기준,
  자정 근처 로그 날짜 밀림 방지). 서버 ts=UTC ISO.
- 순수 로직 comm-log-filter.ts 분리 + 단위테스트 24개
- [내보내기]는 현재 필터 적용 로그만 저장(일자별 저장 가능)

Closes #439

### 6185889c1539e2dfbeab172f4b4299fc434ca800

작성자: vscodereader

Merge remote-tracking branch 'origin/master' into feat/439

### ce272be1490b97f6c21a0f59114cf77d6cab7a4d

작성자: vscodereader

feat(admin): 통신 로그 컴퓨터 라벨을 실제 이름으로 표시(#441 기기 안정식별 반영) [skip ci]

감사로그 device는 device_id(UUID)라 3단 필터의 컴퓨터 목록이 UUID로 보였음.
기기 안정 식별(#441)로 같은 PC=같은 device_id가 되면서, /devices(api.devices.list)의
실제 컴퓨터 이름으로 표시하도록 라벨 해석 추가.

- comm-log-filter.ts: deviceLabel(value, names) — 시스템/등록이름/UUID앞8자리/원문 폴백
- comm-log.tsx: api.devices.list() 폴링으로 id→이름 맵 구성, 목록1·목록2 라벨에 적용
- 테스트 5건 추가(deviceLabel). 프론트 43파일/529 통과.

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문


