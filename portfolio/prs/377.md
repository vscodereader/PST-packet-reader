# PR #377 — fix(admin): 계정 분배 즉시 반영 + 통신로그 스크롤바·위치 유지

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/377
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-02T04:47:52Z
- 병합: 2026-07-03T05:57:41Z
- 브랜치: `fix/admin-distribute-commlog-ux` → `docs/admin-remote-control-design`

## PR 본문 원문

Admin 웹 UX 3건 수정 (베이스: #324 브랜치).

## 계정 분배
- **버그**: 계정 추가(draft) → 전체선택 → 분배 시, draft를 import만 하고 그 새 계정 ID를 분배 호출에 안 넣어 서버가 `account_ids=[]`로 받아 "계정·컴퓨터를 1개 이상 선택하세요"로 거부. 다른 페이지를 갔다 와야(재조회) 풀에 반영돼 그제서야 분배 가능.
- **수정**: import 후 계정 목록을 다시 조회해 방금 넣은 loginId의 서버 ID를 찾아 분배 대상에 합침 → 그 자리에서 즉시 분배. 계정풀 배지도 draft를 즉시 카운트(추가하자마자 총 개수 반영).

## 통신 로그
- 드래그 가능한 스크롤바 상시 표시(`type=always`, `scrollbarSize=12`) — 마우스휠만 쓰던 문제 해결.
- 스크롤 위치 저장/복원 — 다른 페이지 갔다 와도 맨 위로 안 튕기고 마지막 읽던 위치에서 이어 읽음.

검증: `tsc --noEmit` 통과.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## 변경 파일 전체

- `src/admin/features/account-distribute/account-distribute.tsx` — modified, +16/-4
- `src/admin/features/comm-log/comm-log.tsx` — modified, +22/-2
- `src/features/accounts/accounts.tsx` — modified, +15/-1

## 커밋 전체

### a990038b42c3398933f78ea815b8fb4477ee3af0

작성자: vscodereader

fix(admin): 계정 분배 즉시 반영 + 통신로그 스크롤바·위치 유지

- 계정 분배: draft(입력행)를 import 후 새 계정 ID를 다시 조회해 분배 대상에
  포함. 예전엔 import만 하고 ID를 분배에 안 넣어 첫 클릭이 '계정을 선택하세요'로
  거부되고 다른 페이지를 갔다 와야 반영되던 버그 수정. 계정풀 배지도 draft를
  즉시 카운트(추가하자마자 총 개수 반영).
- 통신로그: ScrollArea type=always+scrollbarSize로 드래그 스크롤바 상시 표시.
  스크롤 위치를 모듈 변수에 저장/복원해 화면 전환 후에도 맨 위로 안 튕기고
  마지막 읽던 위치에서 이어 읽게 함.

### 2aa3d0d1e5b7cf71c3570769ef0562bf2d2b774b

작성자: vscodereader

fix(accounts): 계정 목록 주기 갱신 — 원격 등록/자동삭제 즉시 반영

기존엔 마운트 시 1회만 조회해, 에이전트(원격제어)가 계정을 등록하거나 로그인
실패로 자동 삭제해도 다른 페이지를 갔다 와야 보였다. 1.5초 주기로 다시 읽어
등록/삭제가 그 자리에서 바로 반영되게 한다(편집은 키 입력마다 IPC 저장이라
폴링이 값을 덮지 않음).

## 리뷰 원문



## 코드 리뷰 댓글 원문


