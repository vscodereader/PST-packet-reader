# PR #81 — test: add regression suite for #60 review fixes

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/81
- 작성자: vscodereader
- 상태: CLOSED
- 작성: 2026-06-04T03:23:24Z
- 병합: 미병합
- 브랜치: `test/review-regression` → `master`

## PR 본문 원문

## 무엇

사수가 PR #60에 남긴 리뷰 지적들이 **다시 회귀하지 않도록** 통합 회귀 테스트를 추가합니다. 새 파일 `src-tauri/src/review_regression.rs` 한 곳에 모았고, 로그인 폴링 로직을 단위 테스트할 수 있도록 작은 순수 함수 seam을 분리했습니다.

## 커버리지 (리뷰 항목 → 테스트)

- **#1 계정 저장 데이터 손실** → `accounts_merge`: 일부만 저장해도 나머지 보존/upsert/순서/중복 id
- **#2 빈 자격증명** → `credential_guard`: 빈·공백 id/pw 거부, 공백 포함 pw 허용
- **#3 클릭 직후 과도기 신호 latch** → `login_signal_confirmation`: `decide_loop_step`(신규 seam) — 음성 신호 2회 연속 확정, 성공 우선, headed/headless 분기
- **#7 패킷 쿠키 host 누출** → `cookie_host_scoping`: 도메인/host-only 구분, 호스트 간 비누출, 세션 쿠키 전 호스트 적용

(#5 결과 인덱스 매칭·#6 엔드포인트는 프론트엔드라 `publish-modal.test.tsx`/IPC mock에서 커버.)

## 비고

- 프로덕션 변경은 **테스트 가능하게 만든 최소 seam**뿐(로그인 루프 → `decide_loop_step` 추출, 일부 내부 함수 `pub(crate)`). 동작은 동일.
- 기존 인라인 테스트 중복분을 통합 스위트로 이동.
- 로컬: rust 128개 + 프론트 173개 통과, clippy·fmt 클린.

## 변경 파일 전체

- `src-tauri/src/auth/accounts.rs` — modified, +2/-27
- `src-tauri/src/auth/login_flow.rs` — modified, +69/-37
- `src-tauri/src/auth/mod.rs` — modified, +2/-2
- `src-tauri/src/lib.rs` — modified, +4/-0
- `src-tauri/src/naver_automation.rs` — modified, +1/-1
- `src-tauri/src/naver_automation/packet_client.rs` — modified, +16/-44
- `src-tauri/src/review_regression.rs` — added, +283/-0

## 커밋 전체

### 883513eee9e829d5f82b8219c3479fd963255840

작성자: vscodereader

test: add regression suite for #60 review fixes

Consolidate regression tests for the supervisor's PR #60 review points into
src-tauri/src/review_regression.rs, and extract small pure seams so the login
polling logic is unit-testable:

- accounts merge (no data loss when saving a subset)
- empty-credential guard (credentials_present)
- login signal confirmation over two polls (decide_loop_step) — new seam
- packet cookie host-scoping ((domain,name), no cross-host leak)

Inline duplicates moved into the consolidated suite; behavior unchanged.

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문

### vscodereader

인라인 테스트 방식으로 전환합니다. 별도 파일 하나에 모으려면 내부 함수를 pub(crate)로 열어야 해 6개 파일이 바뀌는데, Rust 정석대로 테스트를 코드 옆에 인라인으로 두면 프로덕션 변경이 거의 없어 더 깔끔합니다. (계정 병합·빈 자격증명·쿠키 스코핑 인라인 테스트는 이미 #60으로 master에 있고, 빠진 로그인 신호 확정 테스트만 가볍게 추가합니다.)

https://github.com/beyondsoft-kr/pstmacro/pull/81#issuecomment-4618679341
