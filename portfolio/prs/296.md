# PR #296 — fix(auth): 캡차 첫=즉시 보류 / 보류 재로그인=직접입력(120초), 비번·차단 즉시

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/296
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-06-25T01:59:58Z
- 병합: 2026-06-25T02:00:08Z
- 브랜치: `fix/captcha-onhold-flow` → `master`

## PR 본문 원문

명세(#6 + 사용자 지시)대로 캡차/실패 처리를 분기.

## 변경
- **첫 로그인(일반 계정) 캡차**: grace 없이 **즉시 실패 → 보류(OnHold)** (`FailCaptchaToHold`)
- **보류(OnHold) 계정 재로그인 캡차**: 사용자가 **직접 입력**하도록 창을 성공까지 열어둠
  (`MANUAL_CAPTCHA_TIMEOUT` 120초). 성공 시 활성화·창 닫힘, 초과 시 보류 유지 (`WaitCaptcha`)
- 첫/보류 구분: `process_account`가 IPC 계정 상태(==OnHold)를 조회해 `manual_captcha`로
  login→run→decide_loop_step에 전달
- **비번오류/차단 즉시 확정**(2회 latch 제거) — 결과 DOM 게이트가 과도기 깜빡임을 이미 걸러줌

## 검증
- `cargo test --lib` **908 pass / 0 fail**, clippy(login_flow·login·mod) 깨끗
- ⚠️ 실제 앱 캡차 동작은 **빌드 후 실사용 확인 필요** — 단위/컴파일까지만 검증됨

## 변경 파일 전체

- `src-tauri/src/auth/login.rs` — modified, +10/-4
- `src-tauri/src/auth/login_flow.rs` — modified, +116/-134
- `src-tauri/src/auth/mod.rs` — modified, +23/-7

## 커밋 전체

### c4035500441ef840f415b3cc2ccd802cfa663595

작성자: vscodereader

fix(auth): 캡차 첫=즉시 보류 / 보류 재로그인=직접입력(120초), 비번오류·차단 즉시

명세(#6 + 사용자 지시)대로 캡차/실패 처리를 분기한다:
- 첫 로그인(일반 계정) 캡차: grace 없이 즉시 실패 → 계정 보류(OnHold)
  (decide_loop_step의 FailCaptchaToHold).
- 보류(OnHold) 계정 재로그인 캡차: 사용자가 직접 보안문자를 입력하도록 창을
  성공까지 열어둔다(MANUAL_CAPTCHA_TIMEOUT 120초). 성공 시 활성화·창 닫힘,
  120초 초과 시 보류 유지(WaitCaptcha).
- 첫/보류 구분은 process_account가 IPC 계정 상태(==OnHold)를 조회해 manual_captcha로
  login→run→decide_loop_step에 전달.
- 비번오류/차단은 즉시 확정(2회 latch 제거) — 결과 DOM 게이트가 과도기 깜빡임을
  이미 걸러 즉시여도 안전.

검증: cargo test --lib 908 pass / clippy(login_flow·login·mod) 깨끗.
(실제 앱 캡차 동작은 빌드 후 실사용 확인 필요 — 단위/컴파일까지만 검증됨)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문


