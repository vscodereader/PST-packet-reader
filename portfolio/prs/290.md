# PR #290 — fix(posts): publish-modal NUL 바이트 손상 복구 (#289 회귀)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/290
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-06-24T13:25:21Z
- 병합: 2026-06-24T13:25:44Z
- 브랜치: `fix/publish-modal-nul` → `master`

## PR 본문 원문

PR #289 머지 시 `src/features/posts/publish-modal.tsx` 1525행 내부 dedup 합성 키
`` `${j.loginId}<NUL>${platform}` `` 에 NUL 바이트가 섞여 파일이 바이너리로 깨졌습니다
(프론트 빌드/타입체크 불가). 그 1바이트만 충돌 없는 구분자 `::` 로 복구합니다.

- 동작 변화 없음 — 키는 게시 잡 dedup용 함수 내부 전용.
- 검증: `vitest publish-modal.test.tsx` 52/52 통과, `tsc --noEmit` exit 0.

#286 후속(7건 기능)의 잔여 손상 복구.

## 변경 파일 전체

- `src/features/posts/publish-modal.tsx` — modified, +1/-1

## 커밋 전체

### 626a798bea3102917846a6186705b7ee0c07f5a8

작성자: vscodereader

fix(posts): publish-modal NUL 바이트 손상 복구 (#289 회귀)

PR #289 머지 시 publish-modal.tsx 1525행 합성 키
`${j.loginId}<NUL>${platform}` 에 NUL 바이트가 섞여 파일이
바이너리로 깨졌다(빌드/타입체크 불가). 내부 dedup 키 구분자를
충돌 없는 "::" 로 복구한다. 동작 변화 없음(키는 함수 내부 전용).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문


