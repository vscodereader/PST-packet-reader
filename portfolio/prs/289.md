# PR #289 — feat: 원격제어 준비 전 후속 수정 6건 (큐 가시성·도중차단·로그인 DOM·게시모달·캡차 보류)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/289
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-06-24T13:09:59Z
- 병합: 2026-06-24T13:13:14Z
- 브랜치: `feat/remote-prep-fixes-2` → `master`

## PR 본문 원문

## 요약 (사용자 지시 6건, 기존 코드 재사용·additive)

1. **게시 큐 가시성**: 완료/실패/도중차단 아이템을 큐에서 제거하지 않고 종료(`QueueState::Done`)로 보존 — 여러 작업이 동시에 끝나도 결과를 큐 창에서 바로 보고, ✕·'완료 항목 지우기'로 직접 치운다(`MAX_DONE_NOW` 상한). `pick_next_waiting`은 Waiting만 집어 재실행 없음.
2. **도중 차단 → 차단 상태**: `blocked_post_login_ids`로 게시 도중 차단된 계정은 부분 성공이 있어도 '대기'가 아니라 '차단'으로 전환(차단 우선).
3. **(사수 지시) 로그인 DOM 완전 로드**: 상위 문서뿐 아니라 **모든 iframe까지 readyState=complete + 연속 3회 안정**(`next_ready_streak`)일 때만 타이핑 진행 — 캡차 유발 완화.
4·5. **게시모달 UX**: 게시(N) 시 선택 계정을 그 자리에서 목록에서 제거(`submittedLoginIds`), '계속작성'은 게시설정창 유지 + 계정 목록 새로고침.
6. **캡차 보류**: 로그인 캡차 미해결을 계정 상태 **'보류(onHold)'** 신설 전환(`LoginOutcome::CaptchaUnsolved`), 보류 계정 재로그인은 캡차 10초 대기 → 성공 시 활성화.

## 검증
- 백엔드 `cargo check --tests` 통과, 신규 순수함수 단위 테스트 추가(큐 Done/prune/clear/정렬, blocked_post_login_ids 우선순위, next_ready_streak, CaptchaUnsolved→OnHold).
- 프론트 `vitest` 341 통과, `tsc --noEmit` 통과.

## ⚠️ 검토 요청(범위 밖 동봉분)
- 이 브랜치에는 병렬 작업으로 들어온 **`AccountStatus::TimedOut`(대기초과)** 상태가 함께 있습니다 — 표시 배선은 완비됐으나 아직 **세팅(producer)이 없어 동작하지 않는 미완 스캐폴딩**입니다(#286 후속으로 보임). 기존 코드 삭제 금지 원칙상 제거하지 않고 동봉했으니, 불필요하면 후속에서 정리 바랍니다.
- 기존 `tests/ipc_integration.rs`의 `get_queue_status`/`enqueue_cookie_refresh` 2건은 **master부터 실패**(명령 미등록, 원격제어 선행 테스트)하던 것으로 본 PR과 무관합니다.

Closes #288

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## 변경 파일 전체

- `src-tauri/src/auth/login_flow.rs` — modified, +67/-14
- `src-tauri/src/auth/outcome.rs` — modified, +44/-0
- `src-tauri/src/discussion_batch.rs` — modified, +64/-0
- `src-tauri/src/ipc/accounts.rs` — modified, +20/-0
- `src-tauri/src/ipc/excel.rs` — modified, +4/-0
- `src-tauri/src/ipc/queue.rs` — modified, +179/-8
- `src-tauri/src/ipc/queue_runner.rs` — modified, +282/-16
- `src-tauri/src/lib.rs` — modified, +1/-0
- `src/features/posts/publish-modal.test.tsx` — modified, +43/-0
- `src/features/posts/publish-modal.tsx` — modified, +69/-10
- `src/features/queue/queue.tsx` — modified, +127/-19
- `src/shared/bindings/AccountStatus.ts` — modified, +1/-1
- `src/shared/bindings/QueueState.ts` — modified, +1/-1
- `src/shared/data/config.test.ts` — modified, +8/-0
- `src/shared/data/config.ts` — modified, +10/-0
- `src/shared/data/types.ts` — modified, +5/-1
- `src/shared/ipc/index.ts` — modified, +2/-0

## 커밋 전체

### 4696162ef514ff0c679d0c722ead986440e0dbca

작성자: vscodereader

feat: 원격제어 준비 전 후속 수정 6건 (큐 가시성·도중차단·로그인 DOM·게시모달·캡차 보류)

8계정 병렬 게시 운영 중 나온 후속 수정(사용자 지시). 기존 코드 재사용·additive.

1. 큐 가시성: 완료/실패/도중차단 아이템을 큐에서 제거하지 않고 종료(QueueState::Done)로
   보존 — 여러 작업이 동시에 끝나도 결과를 큐 창에서 바로 보고, ✕·'완료 항목 지우기'로
   직접 치운다(MAX_DONE_NOW 상한). pick_next_waiting은 Waiting만 집어 재실행 없음.
2. 도중 차단(blocked_post_login_ids) 계정은 부분 성공이 있어도 '대기'가 아니라 '차단'으로
   전환(차단 우선) — apply_waiting_for_successful_posts.
3. 로그인 DOM: 상위 문서뿐 아니라 모든 iframe까지 readyState=complete + 연속 3회 안정
   (next_ready_streak)일 때만 타이핑 진행(사수 지시) — 캡차 유발 완화.
4·5. 게시(N) 시 선택 계정을 그 자리에서 목록에서 제거(submittedLoginIds), '계속작성'은
   게시설정창 유지 + 계정 목록 새로고침.
6. 로그인 캡차 미해결을 계정 상태 '보류(onHold)'로 신설 전환(LoginOutcome::CaptchaUnsolved),
   보류 계정 재로그인은 캡차 10초 대기 → 성공 시 활성화.

Closes #288

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### 864e3100f26dd0ebe4093fb035d043273f7598bd

작성자: vscodereader

fix(queue): 종료(done) 상태를 큐 아이템 타입에 추가

Rust QueueState::Done(#1)에 맞춰 수기 유지 타입(QueueNowItem.state)에도 done을 더해 큐 종료 카드 분기가 타입체크되게 한다.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문


