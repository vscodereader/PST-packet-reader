# PR #119 — feat(forum): 종목 선택 라이브 네이버 검색 연결 + 크롤 알림 제거

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/119
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-06-05T07:37:58Z
- 병합: 2026-06-08T05:05:55Z
- 브랜치: `feat/116` → `master`

## PR 본문 원문

Closes #116

## 무엇
- **종목 선택 모달을 라이브 검색에 연결**: 기존 백엔드 `search_stocks`(=`search_naver_stocks`, query 필터 지원)를 `ipc.stocks.search`로 노출하고, 종목 선택 모달이 시드 12개 로컬 필터 대신 이걸 호출. → **아무 종목이나 검색**된다.
- **노이즈 제거**: 가짜 "크롤링" 애니메이션과 "종목 N개 크롤링" 활동 알림(#104가 추가) 삭제.

## 기존 기능 보존 (핵심)
- **백엔드 무변경** (src-tauri 0파일). 검색 로직은 사용자가 구현한 그대로.
- **게시 흐름 정상**: 선택 종목의 `code`만 사용. 라이브 검색 종목 이름은 `onConfirm`으로 받아 게시 작업 `targetName`(알림 표시)에 반영.

## 변경 파일 (프론트만)
- `stock-crawl-modal.tsx` — 라이브 검색 사용으로 재작성(시드/가짜크롤/크롤알림 제거)
- `shared/ipc/index.ts` — `stocks.search` 추가 / `shared/data/types.ts` — `StockCandidate`
- `publish-modal.tsx` — 선택 종목 이름을 targetName에 사용
- `test/ipc.ts` — `search_stocks` mock / `stock-crawl-modal.test.tsx` — 라이브 검색 테스트

## Test plan
- `pnpm typecheck` ✅ / `pnpm test` ✅ **220 passed** / `pnpm lint` ✅
- ⬜ 실기기: 종목 선택 → 검색창에 임의 종목명 입력 → 결과에서 선택 → 게시 (머지 후)

## 비고
- queue.log 가독성(epoch→시각)은 날짜 포맷 의존성 추가가 필요해 본 PR에서 제외, 별도 처리 예정.

## 변경 파일 전체

- `src/features/posts/publish-modal.tsx` — modified, +10/-1
- `src/features/posts/stock-crawl-modal.test.tsx` — modified, +15/-54
- `src/features/posts/stock-crawl-modal.tsx` — modified, +84/-154
- `src/shared/data/types.ts` — modified, +7/-0
- `src/shared/ipc/index.ts` — modified, +6/-1
- `src/test/ipc.ts` — modified, +15/-0

## 커밋 전체

### 17a6dce3639f9d09dd708d9aeb69480c61c1a5cb

작성자: vscodereader

feat(forum): 종목 선택을 라이브 네이버 검색에 연결 + 크롤 알림 제거

- 종목 선택 모달이 시드 12개 로컬필터 대신 기존 백엔드 search_stocks(라이브)를
  호출(ipc.stocks.search). 검색하면 아무 종목이나 결과로 뜸. 백엔드 로직 무변경.
- 가짜 "크롤링" 애니메이션 및 "종목 N개 크롤링" 활동 알림 제거(노이즈).
- 게시 영향 없음: 선택 종목의 code만 사용. 라이브 검색 종목의 이름은 onConfirm으로
  받아 게시 작업 targetName(알림 표시)에 반영.
- typecheck·vitest(220) 통과. queue.log 가독성은 날짜 의존성 필요로 별도 처리.

Closes #116

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문

### pallas-dev · APPROVED



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문


