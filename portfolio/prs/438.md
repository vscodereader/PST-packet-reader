# PR #438 — fix(band): 로그인 폼 네이버 재사용(v4 멈춤 해결)+동의 보강 / feat(admin): 계정상태 하위별 색구분

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/438
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-16T06:45:37Z
- 병합: 2026-07-16T06:45:48Z
- 브랜치: `fix/band-login-reuse-and-account-color` → `master`

## PR 본문 원문

## 1. 밴드 로그인 폼 v4 멈춤 근본 수정 (재사용)
실기기 로그(2026-07-16): 네이버 v3→v4 폼 변경 후 밴드가 자체 복사본이라 안 고쳐져 로그인 폼에서 6분+ 무한 대기 → 자동입력 안 됨 → 동의 클릭 도달 못 함.
- `auth::login_flow::wait_for_login_form`/`click_login_button` pub(crate) 공개
- 밴드는 복사본 버리고 **네이버 구현 직접 재사용** → 앞으로 네이버 폼 바뀌어도 한 곳만 고치면 됨
- 밴드 중복 게이트/상수/헬퍼·테스트 제거
- 동의 버튼 `button.btn.agree`→`button.agree`(실제 클래스) + `form[name=oauthagreeFrm].submit()` 폴백

## 2. 계정상태 하위별 색 구분 (admin)
여러 하위가 전부 보라색이라 헷갈리던 것 → 하위별 고정 색 + 그룹 헤더 구분선 + 색 좌측 테두리.

## 검증
band_auth 52 · auth login_flow 74 · account-state 11 테스트 통과, 빌드·tsc·eslint 에러0. CI 미실행([skip ci]). 반영=하위 COM 앱 재빌드·재설치.

## 변경 파일 전체

- `src-tauri/src/auth/login_flow.rs` — modified, +2/-2
- `src-tauri/src/auth/mod.rs` — modified, +1/-1
- `src-tauri/src/band_auth/login_flow.rs` — modified, +18/-145
- `src/admin/features/account-state/account-state.tsx` — modified, +119/-68

## 커밋 전체

### 0a1f5968eeef2fb7874e9ca4f9e65d2c3130ce65

작성자: vscodereader

fix(band): 로그인 폼 게이트를 네이버 구현 재사용으로 통합(v4 폼 멈춤 해결) + 동의 자동클릭 보강 [skip ci]

증상(실기기 로그 2026-07-16): 네이버 로그인 폼이 v3→v4로 바뀌자, 아침에 네이버(auth::login_flow)만
v4로 고치고 밴드는 자체 복사본이라 안 고쳐져 "[BAND] 로그인 폼 로딩 대기 중"에서 6분+ 무한 대기
→ ID/PW 자동입력 안 됨(수동) → 이후 동의 자동클릭 코드에 도달조차 못 함.

근본 수정 — 중복 제거·단일 소스 재사용:
- auth::login_flow::wait_for_login_form / click_login_button 을 pub(crate) 공개(auth/mod.rs도 공개).
- 밴드는 자체 복사본을 버리고 네이버 구현을 직접 위임 호출 → 앞으로 네이버 폼이 또 바뀌어도 한 곳만
  고치면 밴드도 반영. 오늘 아침 v4 수정(버튼 #loginBtn_column/row · pages/login.js antibot)이 재사용으로
  밴드에 자동 적용.
- 밴드 중복 게이트/상수/헬퍼 제거: login_form_gate_open·next_ready_streak·ANTIBOT_READY_JS·
  RESOURCE_COUNT_JS·FORM_READY_STABLE_POLLS + 관련 테스트 2개(네이버로 일원화). ALL_DOCS_COMPLETE_JS는
  wait_for_dom_ready가 써서 유지.

밴드 전용(네이버엔 없음) 동의 처리 보강:
- 동의 버튼 셀렉터 button.btn.agree → button.agree(실제 클래스, 패킷 oauth_consent.js 확인) +
  form[name=oauthagreeFrm].submit() 폴백(버튼 못 찾으면 동의 폼 직접 제출 → POST allow_oauth).

빌드 경고0, band_auth 52 + auth login_flow 74 테스트 통과. 반영=하위 COM 앱 재빌드·재설치.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### 14616307b794b2f4f3809ae7951e1fbf5b9e1177

작성자: vscodereader

feat(admin): 계정상태 표를 하위 COM별 색·그룹 구분선으로 시각 구분 [skip ci]

여러 하위를 함께 볼 때 전부 같은 보라색이라 어디까지가 어느 COM 계정인지 헷갈리던 문제.
- 하위별 고정 색 배정(선택 순서대로 orange/teal/grape/blue/pink/cyan/lime/indigo)
- 하위별 그룹 헤더 행(색 배경+상단선)으로 "여기부터 이 COM 계정" 구분
- 각 계정 행 좌측에 그 하위 색 3px 세로 테두리 + 하위 배지도 그 색

tsc·eslint 에러0, account-state 11 테스트 통과.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문


