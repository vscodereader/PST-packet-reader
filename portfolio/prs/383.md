# PR #383 — fix(like): 좋아요 실패를 차단(비활성)/세션만료(재로그인)로 구분 + 계정 상태 전환

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/383
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-03T02:21:13Z
- 병합: 2026-07-03T02:21:34Z
- 브랜치: `fix/like-blocked-vs-relogin` → `master`

## PR 본문 원문

## 문제 (사용자 지적)
이전 수정(#382)은 좋아요 실패 계정을 **전부 "재로그인"으로만** 안내하고 **계정 상태를 바꾸지 않았다.**
사용자: "재로그인 떴는데 실제로 로그인해보니 차단된 계정이다. 차단/세션만료 구분해달라고 했잖아.
세션만료는 상태 재로그인으로 바꿔달라고 했잖아. 상태도 안 바뀐다."

## 판별키 (로그 분석 확정)
신버전 로그(`(좋아요)pstmacro.log.2026-07-03`)·게시 로그(`pstmacro.log.2026-07-02`):
- **차단(비활성)** = 세션은 살아있는데(getProfile `rtn_cd:0`) 행동이 막힘 — npay가 `nidlogin.login`으로
  튕기거나, 응답이 **403 `UMON_TEMP/PERMANENT_BANNED`(403F01/403F02)/penaltyDays**. (07-02: 확정 차단
  계정도 getProfile은 rtn_cd:0으로 나옴 → getProfile만으론 구분 불가, UMON/튕김이 판별키.)
- **재로그인** = getProfile가 **로그아웃(`rtn_cd:1`)** = 세션이 죽음. 재로그인해야 회복(재로그인 시
  로그인 플로우가 실제 차단이면 `Blocked`로 확정).

## 변경
- **`AccountStatus::Relogin`("재로그인") 추가** — Rust enum + ts-rs 바인딩(`AccountStatus.ts`) + 프론트
  배지(분홍)/라벨/가이드/문제상태 집계(`config.ts`). `outcome.rs`·`excel.rs`·`stats.rs` 매치 배선.
- **`like_flow`**: `run_naver_like`가 `LikeVerdict`(Liked/Relogin/Blocked/Failed)를 반환하도록 재작성.
  getProfile 로그아웃·쿠키만료 → **Relogin**, npay 로그인튕김(세션 살아있음)·403 UMON → **Blocked**,
  404 글삭제 등 → Failed(상태 안 바꿈).
- **`lib.rs`**: 판정에 따라 계정 상태를 **Relogin/Blocked로 전환**(`apply_status_by_id`) + **쿠키 파일
  삭제**(`clear_account_cookies` → "쿠키만료" 카운트다운 제거·죽은 세션 쿠키 정리 = 사용자 요청 "쿠키기한 삭제").

## 게시 영향 없음
게시 경로(`open_discussion_session`/`ensure_npay_financial_join`) 무변경. 좋아요 경로에만 적용.

## 검증
- `cargo test --lib` **1003 pass**, `tsc --noEmit` OK, `vitest` **359 pass**(config 상태목록 테스트 갱신).
- ⚠️ **실계정 밤샘 재현 검증은 운영자만 가능** — 머지 후 확인 필요(차단 계정=비활성 배지 + 쿠키만료 사라짐 / 만료 계정=재로그인 배지).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## 변경 파일 전체

- `src-tauri/src/auth/accounts.rs` — modified, +12/-0
- `src-tauri/src/auth/mod.rs` — modified, +1/-1
- `src-tauri/src/auth/outcome.rs` — modified, +8/-3
- `src-tauri/src/ipc/accounts.rs` — modified, +24/-0
- `src-tauri/src/ipc/excel.rs` — modified, +1/-0
- `src-tauri/src/ipc/stats.rs` — modified, +4/-1
- `src-tauri/src/lib.rs` — modified, +52/-3
- `src-tauri/src/naver_automation.rs` — modified, +1/-1
- `src-tauri/src/naver_automation/like_flow.rs` — modified, +126/-90
- `src/shared/bindings/AccountStatus.ts` — modified, +1/-1
- `src/shared/data/config.test.ts` — modified, +2/-0
- `src/shared/data/config.ts` — modified, +11/-1
- `src/shared/data/types.ts` — modified, +1/-0

## 커밋 전체

### 2f3017b78f9390298aeee415f5737f11edd0a31a

작성자: vscodereader

fix(like): 좋아요 실패를 차단(비활성)/세션만료(재로그인)로 구분 + 계정 상태 전환

이전 수정(#382)은 좋아요가 안 되는 계정을 전부 "재로그인"으로만 안내하고 계정 상태를 바꾸지
않았다(사용자 지적: 재로그인 떴는데 실제론 차단 계정). 신버전 로그(07-03)·게시 로그(07-02)
분석으로 판별키 확정:
- 차단(비활성): 세션은 살아있으나(getProfile rtn_cd:0) 행동이 막힘 — npay가 nidlogin.login으로
  튕기거나 응답이 403 UMON_*_BANNED(403F01/403F02).
- 재로그인: getProfile가 로그아웃(rtn_cd:1) = 세션이 죽음. 재로그인해야 회복(재로그인 시 로그인
  플로우가 실제 차단이면 Blocked로 확정).

- AccountStatus에 `Relogin`("재로그인") 추가 — Rust enum + ts-rs 바인딩 + 프론트 배지/라벨/가이드/
  문제상태 집계. outcome/excel/stats 매치 배선.
- like_flow: run_naver_like가 LikeVerdict(Liked/Relogin/Blocked/Failed)를 돌려주도록 재작성.
  getProfile 로그아웃·쿠키만료 → Relogin, npay 로그인튕김(세션살아있음)·403 UMON → Blocked,
  404 글삭제 등 → Failed(상태 안 바꿈).
- lib.rs: 판정에 따라 계정 상태를 Relogin/Blocked로 전환하고(apply_status_by_id) 쿠키 파일을
  삭제(clear_account_cookies → "쿠키만료" 카운트다운 제거·죽은 세션 쿠키 정리).
- 게시 경로는 무손대. cargo test 1003 pass, tsc·vitest(359) 통과.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문

### vscodereader

@pallas-dev 사수님, #382 후속입니다. 좋아요 실패를 '차단(비활성)' vs '세션만료(재로그인)'로 구분하고 계정 상태를 실제로 전환합니다(+쿠키 삭제). 판별키는 getProfile(rtn_cd)+403 UMON/npay 튕김. CI(영구 빨강)는 승인대로 무시, merge-commit으로 머지합니다. 실계정 재현 검증은 운영 확인 부탁드립니다.

https://github.com/beyondsoft-kr/pstmacro/pull/383#issuecomment-4872031608
