# PR #445 — feat(admin): 통신로그 기기 2단 필터 — 하위com → 등록 이력(이름·날짜순)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/445
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-20T02:29:12Z
- 병합: 2026-07-20T02:29:15Z
- 브랜치: `feat/444` → `master`

## PR 본문 원문

## Summary
- 통신로그 목록형을 재편: **목록1 컴퓨터(하위comN)** → **목록2 등록 이력(이름·등록일 오름차순)** → **목록3 날짜**.
- 재등록마다 쌓인 랜덤 이름을 하위com(=device_id, #441로 PC당 고정)으로 묶고, 그 컴퓨터의 등록 이름들을 날짜순으로 보여줘 식별 가능하게.

### 서버
- `device_registrations` 이력 테이블 + `register_device`(신규/재사용 양쪽) append + `GET /admin/device-registrations`.
- repo `add/list_device_registrations`(postgres·memory), 마이그레이션·SCHEMA 멱등.

### 프론트
- `api.devices.registrations()` + `comm-log-filter.ts` 순수함수(comOrder/comLabel/registrationsForDevice/regOptionLabel, filterLines 재편) + `comm-log.tsx` 목록형 3단 재편.

### 검증
- server `cargo test` 45(신규 이력 1), 프론트 43파일/519 통과, typecheck·eslint·format 0.

Closes #444

## 전제
machine_id 그룹핑이라 **#441/#442 배포 + 하위앱 재설치 후부터** 실제 하위com 묶임이 채워짐(이력은 배포 후 누적). 그전 등록은 개별.

> ⚠️ CI는 결제 보호로 미실행(`[skip ci]`).

## 변경 파일 전체

- `docs/Admin UI/17-통신로그-기기-2단-필터-등록이력.md` — added, +122/-0
- `server/migrations/0001_init.sql` — modified, +13/-0
- `server/src/model.rs` — modified, +20/-0
- `server/src/repo/memory.rs` — modified, +12/-2
- `server/src/repo/mod.rs` — modified, +6/-1
- `server/src/repo/postgres.rs` — modified, +42/-2
- `server/src/routes.rs` — modified, +77/-0
- `src/admin/api.ts` — modified, +11/-0
- `src/admin/features/comm-log/comm-log-filter.test.ts` — modified, +71/-105
- `src/admin/features/comm-log/comm-log-filter.ts` — modified, +89/-62
- `src/admin/features/comm-log/comm-log.tsx` — modified, +90/-80

## 커밋 전체

### e1b1825805306638ecea637cc5c97292a43b65d2

작성자: vscodereader

feat(admin): 통신로그 기기 2단 필터 — 하위com → 등록 이력(이름·날짜순) [skip ci]

통신로그 목록형을 재편: 목록1 컴퓨터(하위comN) → 목록2 등록 이력(이름·등록일순) → 목록3 날짜.

- 서버: device_registrations 이력 테이블(id·machine_id·device_id·name·registered_at) +
  register_device 신규/재사용 양쪽에서 append + GET /admin/device-registrations.
  repo add/list_device_registrations(postgres·memory), 마이그레이션·SCHEMA 멱등.
- 프론트: api.devices.registrations() + comm-log-filter.ts 순수함수
  (comOrder/comLabel/registrationsForDevice/regOptionLabel/regTimeLabel, filterLines 재편) +
  comm-log.tsx 목록형 3단 재편. 하위com=device_id 단위(#441로 PC당 고정), 목록2=이름 이력 정보표시.
- 검증: server cargo test 45(신규 이력 1), 프론트 43파일/519 통과, typecheck·eslint 0.

전제: machine_id 그룹핑이라 #441/#442 배포 + 하위앱 재설치 후부터 실제 채워짐(이력은 배포 후 누적).

Closes #444

## 리뷰 원문



## 코드 리뷰 댓글 원문


