# PR #435 — fix(cafe): 배포 후 버그 — 최신 댓글이 지정 게시판(menu) 무시하고 카페 전체에 달리던 문제

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/435
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-15T04:50:01Z
- 병합: 2026-07-15T08:40:51Z
- 브랜치: `fix/cafe-latest-comment-menu-scope` → `master`

## PR 본문 원문

## 배포 후 버그 (실기기 발견)
카페 댓글 링크에 특정 게시판(`…/menus/5?viewType=L`)을 넣었는데, **최신 댓글이 그 게시판이 아니라 카페 전체(menuId=0)에서 최신글을 긁어** 엉뚱한 게시판(예: menu 98) 글에 달림. 로그: `article_id=2156949/2156948/2156947` — 카페 전체 최신 글번호(2건은 권한없는 글이라 `COMMENT_HTTP_ERROR` 실패, 1건만 성공).

## 원인 (menu 0 하드코딩 2곳)
1. `agent/mod.rs` build_cafe_publish_items: 최신/인기 댓글 대상을 `cafe_target(…, 0, …)`로 조립(카페 단위 dedup) → NaverTarget.menu_id=0
2. `naver_cafe/article_list/client.rs`: `latest_articles_path`가 `ALL_MENU_ID`(0) 하드코딩

## 수정 (menu_id 관통)
- **최신** = 지정 게시판 단위: `b.menu_id`로 `/cafes/{id}/menus/{menu_id}/articles?sortBy=TIME` 조회, `(카페,게시판)` 조합 dedup
- **인기** = 그대로 카페 전체(주간 인기 API가 카페 단위, menu 무관)
- 링크에 게시판 없으면(menu_id=0) 기존처럼 카페 전체
- 부수 효과: 권한없는 타게시판 글까지 긁어 실패하던 것도 사라짐

## 변경 파일 (전부)
- `src-tauri/src/naver_cafe/article_list/client.rs` — `latest_articles_path`/`latest_referer`/`fetch_article_list`/`fetch_latest_up_to`에 menu_id
- `src-tauri/src/naver_cafe/article_list/service.rs` — `fetch_article_list_for_account`/`fetch_latest_articles_for_account_up_to`에 menu_id
- `src-tauri/src/ipc/queue_runner.rs` — `t.menu_id` 전달
- `src-tauri/src/ipc/cafes.rs` — UI 미리보기는 0(전체) 유지
- `src-tauri/src/agent/mod.rs` — 최신=게시판별, 인기=카페 단위

## 검증
- 빌드 경고·에러 0
- **naver_cafe 350 · queue_runner 119 · agent 51 · article_list 26 테스트 통과**
- CommentTargetSpec/NaverTarget 등 ts-rs 바인딩 구조체 미변경(바인딩 재생성 불필요)

## 변경 파일 전체

- `server/src/hub.rs` — modified, +75/-12
- `server/src/routes.rs` — modified, +9/-3
- `src-tauri/src/agent/mod.rs` — modified, +21/-9
- `src-tauri/src/band_auth/login_flow.rs` — modified, +288/-36
- `src-tauri/src/ipc/cafes.rs` — modified, +1/-1
- `src-tauri/src/ipc/queue_runner.rs` — modified, +16/-7
- `src-tauri/src/naver_cafe/article_list/client.rs` — modified, +21/-24
- `src-tauri/src/naver_cafe/article_list/service.rs` — modified, +5/-2
- `src/admin/features/publish-command/publish-command.test.tsx` — modified, +5/-2
- `src/admin/features/publish-command/publish-command.tsx` — modified, +5/-0

## 커밋 전체

### 6f03ca696cfaf27c28a536813cbd04ecfd573b84

작성자: vscodereader

fix(cafe): 최신 댓글을 지정 게시판(menu) 단위로 조회 — 카페 전체 오달림 수정

링크에 게시판(menus/5)을 넣어도 최신 댓글이 카페 전체(menuId=0)에서 최신글을
긁어 엉뚱한 게시판 글에 달리던 문제. menu 0 하드코딩 2곳(agent 대상조립·article_list
최신경로)을 실제 menu_id로 관통시켜 최신=게시판 단위로 조회한다. 인기글은 주간 API가
카페 전체라 그대로. 권한없는 타게시판 글까지 긁어 COMMENT_HTTP_ERROR 나던 것도 해소.

- article_list client/service: fetch_latest·fetch_article_list에 menu_id 파라미터
- agent build_cafe_publish_items: 최신=b.menu_id, (카페,게시판) dedup / 인기=0
- queue_runner: t.menu_id 전달, ipc/cafes 미리보기는 0(전체) 유지
naver_cafe 350·queue_runner 119·agent 51 테스트 통과, 빌드 경고 0.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### 1c15e9c6e3c791bc9e26889241f5bd7458c68c94

작성자: vscodereader

fix(band): 동의 체크박스를 role=checkbox로 클릭해 전체동의·동의하기 자동화

네이버 OAuth 동의 화면(전체 동의하기 + [필수] 개인정보 제3자 제공)에서 자동 동의가
안 되던 문제. 실측 패킷(밴드 다양한 경우, 2026-07-15)으로 확정: 동의 체크박스는
input[type=checkbox]가 아니라 div[role=checkbox aria-checked](커스텀)이라 예전
조회가 0개를 봐(cbCount:0) 아무것도 못 켜고 동의하기가 막혔다.

- 전체동의 = #agree_select 클릭 → 네이버 JS가 필수(meta-mandatory) 포함 cascade
- 폴백: [role=checkbox] aria-checked=false 직접 클릭(로그인유지 id=keep 제외)
- 동의버튼 = button.btn.agree 최우선 셀렉터(CENTER_JS·폴백)
- click_oauth_consent + handle_signup_final 둘 다 수정
- 첫 인증 계정만 이 화면(POST allow_oauth), 재인증은 GET allow_oauth 자동

빌드 경고0, band_auth 48 테스트 통과. JS 동작은 실기기 검증 필요.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### 81e9aa9b781abc627bbc1641ca502df8a8a9f1a5

작성자: vscodereader

fix(band): 이메일 로그인(본인확인→비번입력→확인) 자동 진행 + 가입 생년월일 날짜 자동선택

배포 후 실기기(오늘 로그 pstmacro.log.2026-07-15)에서 확인된 밴드 로그인 흐름 두 지점 수정.
둘 다 auth.band.us 실측 패킷(밴드 다양한 경우/신규계정 가입, tshark+keylog 복호화)으로 확정.

1) 이메일 로그인 화면 자동 진행 (신규 신호 EmailConfirm/EmailPassword)
   - `confirm_user_email_login`("본인이 맞으신가요?/로그인 하기") → a[href*=email_login/password] 자동 클릭
   - `email_login/password`("비밀번호 입력") → 저장된 비번을 #pw 에 타이핑(실제 keyup 이 band
     checkConfirmButton() 을 돌려 "확인" 활성) → 폼 제출(recaptcha·서명 JS 태움)
   - 비번 오류(#error_msg "계정이 없거나 비밀번호가 일치하지 않습니다.") → BadCredentials 로 확정
     → 기존 "창 닫고 다음 계정" 경로. 제출 직후 recaptcha 대기 중 재입력 방지(band_pw_submitted).
   - 네이버 #id/#pw 폼과 충돌 없음(band 화면은 #pw 만 있고 #id 없음, 오류도 #error_msg 로 분리).

2) 가입 마지막 단계 생년월일 자동선택 (핵심 버그)
   - 실제 폼은 <input type="date" id="bday"> 데이트피커인데, 기존 코드 input 필터가 type=date 를
     제외해 날짜를 못 채웠다(오늘 로그 diag: inputs:[]·selects:[] → 제출버튼 안 열려 34초 헛돎,
     사용자가 수동 입력). band JS 규칙 그대로 재현: #bday=1990-01-01(ISO) + 부모 -active 클래스
     추가(없으면 getBirthdate()가 '' 반환) + input/change 발화 + hidden birthdate=19900101+.
   - diag 로그에 bday/birthdate 값 노출(실기기 검증용).

빌드 경고 0, band_auth 54 테스트 통과(신규 6개 포함). 내 코드 rustfmt 드리프트 0(기존 8곳만).
JS 동작(클릭/타이핑/날짜)은 실기기 검증 필요. 네이버 로그인·동의·가입 기존 경로 무손상.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### 6c6a02f3c2649c6c57f84a7b28f5113f93f76c6c

작성자: vscodereader

fix(remote-control): 밴드 사용전 계정 Admin 노출 + SSE 미연결 분배 명령 큐잉 [skip ci]

배포 후 실기기(comm-log 2026-07-15)에서 확인된 원격제어 2건 수정.

1) 밴드 '사용전' 계정도 Admin에 노출 (카페 패턴 재사용)
   - 밴드는 게시 순간 로그인이라 분배돼도 상태가 new(사용전)에 머물러, filterAccountsByTarget이
     status==active만 보여 안 뜨던 문제. 카페(target==cafe)가 상태 무관 전 계정을 보여주듯,
     밴드도 platform==band 전부를 상태 무관하게 노출(admin/publish-command.tsx). accountRows는
     이미 전 상태를 싣고 있어 프론트 필터만 수정. 테스트 추가(사용전 밴드 노출).

2) SSE 미연결 순간 분배 명령 유실 → 큐잉으로 무손실화 (핵심)
   - 원인: hub.device_push가 SSE 구독자가 그 순간 있어야만(receiver_count>0) 전달, 없으면 버림.
     기기는 heartbeat(별도 POST)로 online이라 Admin이 분배를 허용하지만, 모바일 CGNAT로 SSE
     장수명 연결이 자주 끊겨 그 창에서 분배가 조용히 유실(RESULT 없음). 정오엔 SSE 연결 상태라 성공,
     재시작 후엔 미연결 창에서 유실.
   - 수정: device_push가 미연결이면 버리지 않고 device_id별 pending 큐에 보관(상한 128),
     device_subscribe(재연결) 시 flush하여 방금 붙은 스트림으로 전달. routes.rs [CMD] 로그에
     즉시전달/대기열 여부 표시. hub 단위테스트 2건(오프라인 큐잉→재연결 flush, 연결 시 즉시전달).

server 41 테스트 통과, admin 21 테스트 통과, 빌드 경고 0. 서버/프론트 변경은 Cloud Run 재배포로
반영(프론트 dist가 서버 이미지에 구워짐). 기존 경로 무손상.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문

### vscodereader

## 추가: 밴드 로그인 배포 후 버그 2건 (커밋 81e9aa9)

이 PR은 '배포 후 발생 버그 모음'이라 카페 외 밴드 건도 여기 누적합니다. 오늘 실기기 로그(`pstmacro.log.2026-07-15`) + auth.band.us 실측 패킷(tshark+keylog 복호화)으로 확정.

### 1) 이메일 로그인 화면 자동 진행
네이버 OAuth 통과 후 뜨는 band 자체 화면이 자동화 안 돼 멈추던 문제.
- `confirm_user_email_login`("본인이 맞으신가요?/로그인 하기") → `a[href*=email_login/password]` 자동 클릭
- `email_login/password`("비밀번호 입력") → 저장 비번을 `#pw`에 타이핑(실제 keyup이 band `checkConfirmButton()` 돌려 "확인" 활성) → 폼 제출(recaptcha·서명 JS 태움)
- 비번 오류(`#error_msg` "계정이 없거나 비밀번호가 일치하지 않습니다.") → BadCredentials 확정 → 기존 '창 닫고 다음 계정' 경로
- 네이버 `#id/#pw` 폼과 무충돌(band 화면은 `#pw`만 있고 `#id` 없음)

### 2) 가입 마지막 단계 생년월일 자동선택 (핵심)
실제 폼은 `<input type="date" id="bday">` 데이트피커인데 기존 코드가 `type=date`를 필터에서 제외 → **날짜 미입력으로 제출버튼 안 열려 34초 헛돎**(오늘 로그 diag `inputs:[]·selects:[]`, 사용자가 수동 입력).
- band JS 규칙 재현: `#bday=1990-01-01`(ISO) + 부모 `-active` 클래스(없으면 `getBirthdate()`가 '' 반환) + input/change 발화 + hidden `birthdate=19900101+`

빌드 경고 0, band_auth 54 테스트 통과(신규 6개). 네이버 로그인·동의·가입 기존 경로 무손상. **JS 동작(클릭/타이핑/날짜)은 실기기 검증 대기.**

https://github.com/beyondsoft-kr/pstmacro/pull/435#issuecomment-4978199745
