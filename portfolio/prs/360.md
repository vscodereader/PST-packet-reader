# PR #360 — fix(post): 종토방 댓글 계정별 3초 간격 스로틀 (도배방지·In process 회피)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/360
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-01T02:38:29Z
- 병합: 2026-07-01T02:38:31Z
- 브랜치: `fix/forum-comment-3s-throttle` → `master`

## PR 본문 원문

"특정 게시글" 댓글을 여러 개 달 때 같은 계정이 **동시에(병렬 Chrome)** 여러 글에 댓글을 달아, 네이버가 **도배방지(code 5010)·"In process"(code 8001)** 로 막던 문제(실측 로그 2026-07-01).

## 수정
전역 **계정별 예약대장**으로 같은 계정 댓글을 **최소 3초 간격으로 직렬화**:
- 같은 계정: `now, now+3s, now+6s…` → **댓글 1개 → 3초 → 다음 댓글**
- 다른 계정: 서로 독립 → **여러 계정이 각자 3초 간격으로 병렬** 진행(종토방 글 병렬과 동일 UX)
- 락은 슬롯 계산 동안만, 대기는 락 밖 `sleep` → 다른 계정 안 막음
- 글만 올리는 작업(`run_comment=false`)은 스로틀 미적용

## 절대경로 (특정 게시글 댓글 경로)
- plan→요청: `src-tauri/src/ipc/queue_runner.rs:1233` `plan_to_forum_requests` (URL마다 별도 요청)
- 병렬 실행: `queue_runner.rs` `run_forum_targets` (계정별 Chrome)
- **스로틀 추가 지점**: `src-tauri/src/discussion_batch.rs` `run_one_forum_stock` (댓글 포함 시 `throttle_account_comment`)
- 실제 댓글 API: `src-tauri/src/naver_automation/packet_client.rs` `submit_comment`

## 검증
`cargo test discussion_batch` 13 passed(순수 슬롯 로직 단위 테스트 포함) · `cargo check` 경고0.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## 변경 파일 전체

- `src-tauri/src/discussion_batch.rs` — modified, +70/-1

## 커밋 전체

### ffeebd86a97c472ce98e19a29b6c7e5320ecbcb3

작성자: vscodereader

fix(post): 종토방 댓글 계정별 3초 간격 스로틀 — 도배방지(5010)·In process(8001) 회피

"특정 게시글" 댓글은 URL마다 별도 요청(plan_to_forum_requests)이라 같은 계정이 여러
게시글에 동시에(병렬 Chrome) 댓글을 달았고, 네이버가 도배방지(code 5010)·"In process"
(code 8001)로 막았다(실측 로그 2026-07-01, cho 계정 3개 동시).

전역 계정별 예약대장(COMMENT_NEXT_ALLOWED)으로 같은 계정 댓글을 최소 3초 간격으로
직렬화한다: 같은 계정 N건이 동시에 들어와도 now, now+3s, now+6s… 슬롯을 잡아 "댓글 1개
→ 3초 → 다음 댓글"이 된다. 계정끼리는 독립이라 여러 계정이 각자 3초 간격으로 병렬 진행
(종토방 글 병렬과 동일 UX — 사용자 요청). 락은 슬롯 계산 동안만, 대기는 락 밖 sleep이라
다른 계정을 막지 않는다. 글만 올리는 작업(run_comment=false)은 스로틀 안 탄다.

순수 슬롯 로직(reserve_comment_slot) 분리 + 단위 테스트: 같은 계정 3초씩 간격, 다른
계정 독립, 과거 예약은 now부터.

검증: cargo test discussion_batch 13 passed · cargo check 경고0.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문


