# PR #363 — fix(post): npay 필수약관 콜백 따라가기 + 동의 Y + 프로필 상태 500 진단 프로브

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/363
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-01T04:16:27Z
- 병합: 2026-07-01T04:21:10Z
- 브랜치: `fix/npay-mandatory-term-consent` → `master`

## PR 본문 원문

## Summary
- 종토 게시가 특정 계정에서 **프로필 상태 500**(Failed to fetch profile user status)으로 매번 실패.
- 실측 패킷(`프로필 생성` 캡처)으로 **그 계정은 npay 완료 상태**이고, 브라우저에선 같은 `GET /status`가 **200 nonExistent**임을 확인 → 500은 npay/프로필생성 결함이 아니라 **우리 앱 요청(헤더/쿠키) 차이**일 가능성이 큼.
- **npay 필수약관 콜백 보강**(진짜 미가입 fresh 계정용): `commonTermAgree`(200 HTML+JS 콜백)에서 멈추지 말고 `rurl`(약관동의 콜백)을 이어서 GET → 가입 완료. 순수 함수 `term_agree_callback_url` + 단위테스트.
- **동의 4종 전부 Y**(성공 브라우저 캡처와 100% 동일, 사용자 지시).
- **[진단·임시] `debug_probe_profile_status`**: 본 status 요청 직전에 브라우저와 동일 헤더(origin 제거·sec-ch-ua·referer 루트)로 한 번 더 쏴서 결과 로그. **프로브=200 & 본요청=500 → 원인=헤더 / 프로브=500 → 원인=쿠키·세션.** 원인 확정 후 제거 예정.

## Test plan
- [x] `cargo test --lib packet_client::tests` 25 passed (신규 `term_agree_callback_url_extracts_rurl` 포함)
- [x] `cargo build --lib` 무경고 · rustfmt 내 라인 clean
- [ ] 실기기: 실패 계정(zip)으로 1회 게시 → 로그의 `[진단] 프로필 상태 프로브` 값 확인(200/500)
- [ ] CI green (사수 결제미납 항상 빨강 — `--admin` merge-commit)

## 변경 파일 전체

- `src-tauri/src/naver_automation/packet_client.rs` — modified, +135/-4

## 커밋 전체

### 44e7c5aa9507590e979d287d155072090d853e28

작성자: vscodereader

fix(post): npay 필수약관 콜백 따라가기 + 동의 Y + 프로필 상태 500 진단 프로브

종토 게시가 특정 계정에서 "프로필 상태 500(Failed to fetch profile user status)"으로
매번 실패. 실측 패킷(`프로필 생성`)으로 그 계정은 npay 완료 상태이고 브라우저에선 같은
GET /status가 200 nonExistent임을 확인 — 즉 500은 npay/프로필생성 문제가 아니라 우리 앱이
보내는 요청(헤더/쿠키) 차이일 가능성이 크다.

변경:
- financial_join_follow: 필수약관 페이지(commonTermAgree, 200 HTML+JS 콜백)에서 멈추지 말고
  URL의 rurl(=약관동의 콜백)을 이어서 GET → 진짜 미가입 fresh 계정의 가입을 완료시킨다.
  순수 함수 term_agree_callback_url + 단위테스트.
- FINANCIAL_JOIN_URL 동의 4종 전부 Y(성공 브라우저 캡처와 동일, 사용자 지시).
- [진단·임시] ensure_profile_intro_setup 진입 시 debug_probe_profile_status: 본 요청 직전에
  브라우저와 동일 헤더(origin 제거·sec-ch-ua·referer 루트)로 status를 한 번 더 쏴 결과를 로그.
  프로브=200 & 본요청=500 → 원인=헤더 / 프로브=500 → 원인=쿠키·세션. 원인 확인 후 제거 예정.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문


