# PR #358 — fix(posts): 특정 게시글 댓글 링크 여러 개가 1개로 줄던 버그 (백엔드 저장 누락)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/358
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-01T02:03:14Z
- 병합: 2026-07-01T02:03:16Z
- 브랜치: `fix/comment-urls-persist` → `master`

## PR 본문 원문

작성 모달에서 **특정 게시글 댓글 링크를 3개 넣고 저장해도 다시 열면 1개만** 남던 문제(실측 로그 2026-07-01 — 댓글도 1개 글에만 달림).

## 원인
프론트는 `commentUrls`(여러 개)를 저장하는데, 백엔드 `LibraryPost` 구조체에 `comment_urls` 필드가 **없어서** serde가 저장 시 통째로 버렸습니다. 다시 읽으면 프론트가 `commentUrl`(단수) 1개로 폴백 → **링크 1개만 남음**.

## 수정
- `LibraryPost`에 `comment_urls: Option<Vec<String>>` 추가(`commentUrls`, ts optional). `comment_url`(단수)은 하위호환 유지(항상 `commentUrls[0]`).
- 구조체 리터럴 7곳(seed 4·excel 2·test 1)에 `comment_urls: None` 채움.
- TS 바인딩 `LibraryPost.ts`에 `commentUrls?: Array<string>` 추가(수기).
- 회귀 테스트: `commentUrls` 3개가 역직렬화→직렬화 라운드트립에서 유지되는지 검증.

## 검증
`cargo test ipc::posts` 7 passed · `tsc` 0 · `vitest` writer/posts 36 passed ✅

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## 변경 파일 전체

- `src-tauri/src/ipc/excel.rs` — modified, +2/-0
- `src-tauri/src/ipc/posts.rs` — modified, +41/-0
- `src/shared/bindings/LibraryPost.ts` — modified, +1/-1

## 커밋 전체

### 061fb8bb798c7d4c1978ac83d8a5da882bfcabe1

작성자: vscodereader

fix(posts): 특정 게시글 댓글 링크 여러 개가 1개로 줄던 버그 — 백엔드 commentUrls 저장 누락

작성 모달에서 특정 게시글 댓글 링크를 3개 넣고 저장해도 다시 열면 1개만 남던 문제.
원인: 프론트는 commentUrls[여러개]를 보내는데 백엔드 LibraryPost 구조체에 comment_urls
필드가 없어 serde가 저장 시 통째로 버렸다. 다시 읽으면 프론트가 commentUrl(단수) 1개로
폴백 → 링크 1개만 남고, 게시 때도 그 1개 글에만 댓글이 달렸다(실측 로그 2026-07-01).

- LibraryPost에 comment_urls: Option<Vec<String>> 추가(camelCase→commentUrls, ts optional).
  comment_url(단수)은 하위호환으로 유지(항상 commentUrls[0]).
- 생성 리터럴 7곳(seed 4·excel 2·test 1)에 comment_urls: None 채움.
- TS 바인딩 LibraryPost.ts에 commentUrls?: Array<string> 추가(수기).
- 회귀 테스트: commentUrls 3개가 역직렬화→직렬화 라운드트립에서 유지되는지 검증.

검증: cargo test ipc::posts 7 passed · tsc 0 · vitest writer/posts 36 passed.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문


