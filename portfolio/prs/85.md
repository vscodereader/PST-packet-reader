# PR #85 — fix(auth): incognito login, 1s airplane toggle, log IP before/after

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/85
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-06-04T04:46:54Z
- 병합: 2026-06-04T04:46:58Z
- 브랜치: `fix/adb-ip-log-incognito` → `master`

## PR 본문 원문

## 무엇 (실계정 테스트 피드백)

1. **비행기모드 ON 시간 2s → 1s** (`ADB_AIRPLANE_ENABLE_SECS`).
2. **IP 회전 가시화**: `toggle_airplane_mode`가 토글 전후 외부 IP를 콘솔에 `기존 IP / 바뀐 IP`로 출력. 같으면 경고(테더링이 기본 경로 아님 / 통신사 CGNAT). best-effort(blocking reqwest를 spawn_blocking으로).
3. **시크릿 로그인**: 로그인 Chrome을 `--incognito`로 띄움(계정마다 깨끗한 세션).

> 참고: Samsung One UI는 `cmd connectivity airplane-mode`로 토글해도 상단 버튼 불이 안 들어올 수 있음 — IP가 바뀌면 라디오는 실제로 순환한 것.

check·128+8 테스트·clippy·fmt 통과.

## 변경 파일 전체

- `src-tauri/src/auth/adb.rs` — modified, +38/-3
- `src-tauri/src/auth/chrome.rs` — modified, +2/-0
- `src-tauri/src/auth/config.rs` — modified, +1/-1

## 커밋 전체

### 897149885e9ed7ab02baf9015bbb382bf4099297

작성자: vscodereader

fix(auth): incognito login, 1s airplane toggle, log IP before/after

- Airplane-mode ON duration 2s -> 1s (ADB_AIRPLANE_ENABLE_SECS).
- toggle_airplane_mode logs the external IP before and after the toggle
  (기존 IP / 바뀐 IP) so IP rotation is visible in the dev console, with a
  warning when it stays the same (tether-not-default-route / carrier CGNAT).
- Launch the login Chrome with --incognito (clean session per account).

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문


