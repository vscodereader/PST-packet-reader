# PR #437 — fix: 원격제어 분배 지연 + Admin UX(분배 버튼·미리보기 제거·계정상태 다기기)

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/437
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-16T05:07:55Z
- 병합: 2026-07-16T05:08:25Z
- 브랜치: `fix/remote-control-and-admin-ux` → `master`

## PR 본문 원문

## 배포 후 개선 묶음 (2026-07-16)

### 1. 원격제어 계정 분배 지연 수정 (server + agent)
IP 회전 잦은 하위는 SSE가 계속 끊겨 분배 명령이 4분+ 지연되던 문제. 하트비트 pull(대기 명령을 하트비트 응답에 실어 전달)+SSE 재연결 단축(30→5s)+commandId 중복무시. 기존 SSE 경로 무손대, 추가만.

### 2. 계정 분배 버튼 분리 (admin)
- **분배하기**: 1대 전용(그 1대에 전부) — 고장 하위에 유실 방지
- **나눠서 분배하기**: 2대 이상 균등 분할(기존). 서버 무변경.

### 3. '미리보기' 제거 (admin)
배포 완료로 개발용 화면 선택기 제거.

### 4. 계정상태 다기기 + 삭제 버그 (admin)
- 여러 하위 동시 선택, 저장은 하위별 독립 명령(B 변경이 A로 안 감)
- 낙관적 삭제 후 폴링이 지운 계정 되살리던 버그 수정(pendingDeletes 필터)

## 검증
server 41 · agent 51 · account-state 11 테스트 통과, tsc·eslint 에러0. CI 미실행([skip ci]).
반영: 서버=Cloud Run 재배포, admin/agent=재배포·재빌드.

## 변경 파일 전체

- `server/src/hub.rs` — modified, +9/-0
- `server/src/routes.rs` — modified, +21/-1
- `src-tauri/src/agent/mod.rs` — modified, +128/-59
- `src-tauri/src/agent/net.rs` — modified, +25/-10
- `src/admin/admin-app.tsx` — modified, +3/-51
- `src/admin/features/account-distribute/account-distribute.tsx` — modified, +34/-11
- `src/admin/features/account-state/account-state.tsx` — modified, +221/-100

## 커밋 전체

### 54536c83a66114fa6ff5fc12b7c31f7bd663a82d

작성자: vscodereader

fix(remote-control): 분배 명령 지연 수정 — 하트비트 pull + SSE 재연결 단축 + 중복무시 [skip ci]

증상(comm-log 2026-07-16): 재등록/IP회전 잦은 하위는 SSE가 계속 끊겨 계정 분배 명령이 4분+ 늦게
도착. 하트비트로는 online이라 Admin은 분배 허용하지만 명령 채널(SSE)이 죽어 전달 지연.

수정(기존 SSE 경로 무손대, 추가만):
- 서버 hub.drain_pending + 하트비트 응답에 대기 명령 실어보냄(routes.rs) — SSE가 못 붙어도
  하트비트(30초·IP무관 항상 성공)로 확실히 전달. device_subscribe(SSE flush)와 같은 pending 큐
  공유라 이중 전달 없음.
- 에이전트: 하트비트 응답 명령을 SSE와 동일 경로(process_command)로 처리(net.rs·mod.rs).
- 안전장치: commandId 중복무시(mark_command_seen, 최근512)로 SSE·하트비트 겹쳐도 1회만 실행.
- SSE 재연결 backoff 상한 30초→5초(IP회전 후 빨리 복구).

server 41 + agent 51 테스트 통과, 빌드 경고0. 반영=Cloud Run 재배포(서버)+하위앱 재빌드(에이전트).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### a9ddff4faf6bdded926f87d79aa00ba9a91d6af3

작성자: vscodereader

feat(admin): 계정 분배 '분배하기'(1대 전용)·'나눠서 분배하기'(균등분할) 버튼 분리 [skip ci]

고장/먹통 하위에 몫이 흘러가 유실되는 것을 피하려면 믿는 1대만 골라 전부 줄 수 있어야 함.
- 분배하기: 컴퓨터 정확히 1대 선택 시 활성 → 그 1대에 전부.
- 나눠서 분배하기: 2대 이상 선택 시 활성 → 계정을 대수로 균등 분할(기존 동작).
서버 무변경 — distribute()가 이미 1대=전부/N대=분할 처리. 활성화 조건만 분리(순수 UI). tsc 통과.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### ea1cad95ebad6834f418932168a3c68d5a0af35a

작성자: vscodereader

chore(admin): 배포 완료 — 상단 '미리보기' 화면 선택기 제거 [skip ci]

실서비스 배포됐으므로 인증 없이 아무 화면이나 띄우던 개발용 PreviewSwitcher(로그인 전 우상단 +
앱 헤더) 제거. 안 쓰게 된 import(Badge·Select·PREVIEW_SCREENS) 정리. 로그인 성공 시 go("devices")로
정상 진입하므로 네비게이션 영향 없음. tsc·eslint 통과.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

### 8eb1df7d52b3a2672a819b304813818b6272321e

작성자: vscodereader

feat(admin): 계정상태 다기기 동시 선택 + 하위별 독립 저장 + 삭제 되살아남 버그 수정 [skip ci]

- 여러 하위 COM 동시 선택(MultiSelect): 선택한 모든 하위 계정을 한 표에 '하위' 열과 함께 표시.
- 저장은 하위별로 각자 따로 명령(updateMeta를 기기별 호출) — B 변경이 A로 가지 않음. 편집·삭제도
  deviceId::loginId로 분리해 하위 간 같은 loginId가 섞이지 않음.
- 삭제 되살아남 버그 수정: 낙관적 삭제 후 하위가 삭제 반영 전이라 4초 폴링이 그 계정을 되살리던 문제.
  pendingDeletes(ref)로 폴링 결과에서 걸러내고, 하위가 더 이상 보고 안 하면 해제(재등장 종료).

순수함수 diffAccountRows·삭제 배선 테스트 11개 통과, tsc·eslint 에러0.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문


