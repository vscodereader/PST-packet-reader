# PR #285 — feat(queue): 동시 작업 수 무제한화 + 최대 작동가능 작업 수 설정

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/285
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-06-24T10:01:13Z
- 병합: 2026-06-24T10:01:16Z
- 브랜치: `feat/284` → `master`

## PR 본문 원문

Closes #284

@pallas-dev

now 큐 동시 실행 상한(FORUM 4·COMMENT 6 고정)을 **기본 무제한**으로 바꾸고, 큐 화면에서 사용자가 최대 동시 작업 수를 지정할 수 있게 함.
- `ConcurrencyConfig{limit:u32}`(0=무제한) 영속화 + `get/set_now_concurrency_limit` IPC.
- 워커가 claim 시점마다 한도를 동적 조회(`may_claim`) → **한도를 낮춰도 이미 도는 작업은 안 멈추고, 새 claim만 active<limit까지 대기**.
- 큐 화면 '드래그로 순서 변경' 아래 '최대 작동가능 작업 수' 입력+저장. 빈칸/0=무제한.

검증: cargo --lib **888** / vitest **339** / tsc **0** / clippy **깨끗**.

## 변경 파일 전체

- `src-tauri/src/ipc/queue_runner.rs` — modified, +79/-12
- `src-tauri/src/lib.rs` — modified, +27/-0
- `src/features/queue/queue.test.tsx` — modified, +48/-0
- `src/features/queue/queue.tsx` — modified, +62/-0
- `src/shared/ipc/index.ts` — modified, +10/-0
- `src/test/ipc.ts` — modified, +8/-0

## 커밋 전체

### 8520ffdcdd731efc9c3b74c0cadf0ad0e97da611

작성자: vscodereader

feat(queue): 동시 작업 수 무제한화 + 사용자 설정(최대 작동가능 작업 수) (#284)

now 큐 동시 상한(FORUM 4·COMMENT 6 고정)을 제거하고 기본 무제한으로 바꾼다.
- ConcurrencyConfig(limit:u32, 0=무제한) JsonStore 영속화 + get/set IPC.
- 워커는 claim 시점마다 read_concurrency_limit로 동적 조회 → may_claim(active,limit).
  한도를 낮춰도 이미 도는 작업(active 반영)은 안 멈추고 새 claim만 active<limit까지 대기.
- 큐 화면 '드래그로 순서 변경' 아래에 '최대 작동가능 작업 수' 입력+저장 버튼. 빈칸/0=무제한.

기존 카페·종토·밴드·블로그·예약·나눠서 무손상.
검증: cargo --lib 888 / vitest 339 / tsc 0 / clippy 깨끗.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문


