# PR #366 — fix(post): 프로필 상태 500을 상황별로 구분 — 계정 보호조치 vs npay 미완료 vs 그 외

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/366
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-01T05:26:06Z
- 병합: 2026-07-01T05:35:05Z
- 브랜치: `fix/profile-500-clarify-blocked-vs-npay` → `master`

## PR 본문 원문

## Summary
- **증상(사용자 지적)**: 원인이 서로 다른데도(①npay 미완료 ②계정 보호조치·세션무효 ③그 외) 전부 똑같이 **"프로필 상태 500 / Failed to fetch profile user status"** 로만 떠서, 로그만으론 계정이 막힌 건지 npay가 안 된 건지 구분 불가(실측: `kkch****`가 보호조치로 막힌 뒤에도 같은 500만 보임).
- **수정**: npay 가입(`financial_join_follow`, **패킷**)의 최종 착지로 원인을 판정(`NpayJoinStatus`)하고, 프로필 상태 500 시 메시지를 상황별로 바꾼다(`clarify_profile_status_error`):
  - **LoginRequired**(nid가 `nidlogin.login`으로 튕김=인증 거부) → "계정 세션 무효/**보호조치** 추정 — 재로그인 필요" **차단성** → `is_blocking_failure`로 분류돼 이 계정 남은 글 **건너뜀**(어차피 전부 500).
  - **TermsPending**(commonTermAgree) → "npay 필수약관 미완료 — 재로그인 시 자동 가입(#364)" 안내(차단 오분류 방지).
  - **Completed/Unknown** + 500 → 원본 유지(진짜 다른 프로필 문제).
- **게시는 그대로 100% 패킷** — 페이지 이동/로드 대기 없음. 추가 API 호출도 없음(npay 판정 재사용) → 시간 증가 없고, 막힌 계정은 skip으로 오히려 빠름.

## Test plan
- [x] `cargo test --lib` 986 passed (신규 `clarify_profile_status_error_differs_by_npay_status` + `is_blocking_failure("보호조치")`)
- [x] `cargo build --lib` OK · rustfmt 내 라인 clean · clippy 신규 경고 없음
- [ ] 실기기: 보호조치 계정 → "보호조치/재로그인 필요"로 뜨고 남은 글 skip / npay 미완료 계정 → "npay 미완료" 안내
- [ ] CI green (사수 결제미납 항상 빨강 — `--admin` merge-commit)

## 변경 파일 전체

- `src-tauri/src/discussion_batch.rs` — modified, +8/-1
- `src-tauri/src/naver_automation.rs` — modified, +100/-5
- `src-tauri/src/naver_automation/packet_client.rs` — modified, +29/-3

## 커밋 전체

### 447c574ce784584c0c57caa758cdd8399e5c99c5

작성자: vscodereader

fix(post): 프로필 상태 500을 상황별로 구분 — 계정 보호조치 vs npay 미완료 vs 그 외

증상(사용자 지적): 원인이 서로 다른데도(①npay 미완료 ②계정 보호조치·세션무효 ③그 외)
전부 똑같이 "프로필 상태 500 / Failed to fetch profile user status"로만 떠서, 로그만 보고는
계정이 막힌 건지 npay가 안 된 건지 구분할 수 없었다(실측: kkch****가 보호조치로 막힌 뒤에도
같은 500만 보임). 게시는 그대로 100% 패킷 — 페이지 이동/로드 대기 없음.

수정: npay 가입(financial_join_follow, 패킷)의 최종 착지로 원인을 판정(NpayJoinStatus)하고,
프로필 상태 500이 나면 그 판정에 따라 메시지를 바꾼다(clarify_profile_status_error):
- LoginRequired(nid가 nidlogin.login으로 튕김=인증 거부): "계정 세션 무효/보호조치 추정 —
  재로그인 필요" 차단성 메시지 → is_blocking_failure("보호조치")로 분류돼 이 계정 남은 글은
  건너뛴다(어차피 전부 500).
- TermsPending(commonTermAgree): "npay 필수약관 미완료 — 재로그인 시 자동 가입(#364)" 안내
  (차단 마커는 피해 계정을 '차단'으로 오분류하지 않음).
- Completed/Unknown + 500: 원본 메시지 유지(진짜 다른 프로필 문제).
추가 API 호출 없음(npay 판정 재사용) → 시간 증가 없고, 막힌 계정은 남은 글 skip으로 오히려 빠름.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문


