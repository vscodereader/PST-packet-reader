# PR #385 — feat(login): 수동추가 시 ADB 연결되면 IP 한 번 회전 후 로그인창

- 원본: https://github.com/beyondsoft-kr/pstmacro/pull/385
- 작성자: vscodereader
- 상태: MERGED
- 작성: 2026-07-03T03:05:11Z
- 병합: 2026-07-03T03:05:21Z
- 브랜치: `feat/manual-add-adb-rotate` → `master`

## PR 본문 원문

## 요약
수동추가(사람이 직접 로그인)는 지금까지 IP 회전 없이 바로 로그인창을 띄웠다.
**ADB가 연결(확인)돼 있으면** 로그인창을 띄우기 **전에 IP를 한 번 회전**하고, **없으면 기존과 동일하게** 그대로 진행한다. 그 이후 흐름은 전부 기존과 동일.

## 구현
일반 로그인의 '있으면 회전, 없으면 생략' 블록(`auth/mod.rs §79`)을 그대로 재사용 — `probe_adb_connection` → `toggle_airplane_mode` → 안정화 대기. `manual_add_account`에 if 하나 추가.

## 검증
`cargo check` OK · `cargo test --lib auth` 124 pass. 기존 코드 재사용, 동작 무변경(ADB 없을 때).

🤖 Generated with [Claude Code](https://claude.com/claude-code)


## 변경 파일 전체

- `src-tauri/src/auth/mod.rs` — modified, +18/-0

## 커밋 전체

### 094e2e34b24816362dd1d432350c47ed0ef5daba

작성자: vscodereader

feat(login): 수동추가 시 ADB 연결돼 있으면 IP 한 번 회전 후 로그인창

수동추가는 지금까지 IP 회전 없이 바로 로그인창을 띄웠다. ADB가 연결(확인)돼
있으면 창을 띄우기 전에 IP를 한 번 회전한다. 없으면 기존과 동일하게 그대로 진행.

- 일반 로그인의 '있으면 회전, 없으면 생략' 블록(auth/mod.rs §79)을 그대로
  재사용: probe_adb_connection → toggle_airplane_mode → 안정화 대기.
- 그 이후 흐름은 전부 기존과 동일. cargo test auth 124 pass.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

## 리뷰 원문



## 코드 리뷰 댓글 원문



## PR 대화 댓글 원문


